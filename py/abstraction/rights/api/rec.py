# Code generated by abstraction-idl from abstraction-rights/rights.thrift. DO NOT EDIT.

_HEX = b"0123456789abcdef"
_WS = (0x20, 0x09, 0x0A, 0x0D)
_SHORT = {0x22: b'\\"', 0x5C: b"\\\\", 0x08: b"\\b", 0x0C: b"\\f",
          0x0A: b"\\n", 0x0D: b"\\r", 0x09: b"\\t"}


def esc_byte(out, c):
    short = _SHORT.get(c)
    if short is not None:
        out += short
    elif c < 0x20:
        out += b"\\u00" + bytes([_HEX[c >> 4], _HEX[c & 0xF]])
    else:
        out.append(c)


def num(out, n):
    out += str(int(n)).encode("ascii")


def pad(out, depth):
    out += b" " * (depth * 2)


def strs(out, v, depth):
    if not v:
        out += b"[]"
        return
    out += b"[\n"
    for i, s in enumerate(v):
        pad(out, depth + 1)
        esc(out, s)
        if i + 1 < len(v):
            out += b","
        out += b"\n"
    pad(out, depth)
    out += b"]"


def raw(out, s, depth):
    b = s.encode("utf-8") if isinstance(s, str) else s
    i, n = 0, len(b)
    while i < n:
        c = b[i]
        if c in _WS:
            i += 1
        elif c == 0x22:
            j = i + 1
            while j < n:
                if b[j] == 0x5C:
                    j += 2
                    continue
                if b[j] == 0x22:
                    j += 1
                    break
                j += 1
            out += b[i:j]
            i = j
        elif c in (0x7B, 0x5B):
            out.append(c)
            i += 1
            j = i
            while j < n and b[j] in _WS:
                j += 1
            if j < n and b[j] in (0x7D, 0x5D):
                out.append(b[j])
                i = j + 1
            else:
                depth += 1
                out += b"\n"
                pad(out, depth)
        elif c in (0x7D, 0x5D):
            depth -= 1
            out += b"\n"
            pad(out, depth)
            out.append(c)
            i += 1
        elif c == 0x2C:
            out += b",\n"
            pad(out, depth)
            i += 1
        elif c == 0x3A:
            out += b": "
            i += 1
        else:
            out.append(c)
            i += 1


def rawmap(out, m, depth):
    keys = sorted(m, key=lambda k: k.encode("utf-8"))
    if not keys:
        out += b"{}"
        return
    out += b"{\n"
    for i, k in enumerate(keys):
        pad(out, depth + 1)
        esc(out, k)
        out += b": "
        raw(out, m[k], depth + 1)
        if i + 1 < len(keys):
            out += b","
        out += b"\n"
    pad(out, depth)
    out += b"}"

def esc(out, s):
    out += b'"'
    for c in s.encode("utf-8"):
        esc_byte(out, c)
    out += b'"'


def enc_list(out, v, depth, enc):
    if not v:
        out += b"[]"
        return
    out += b"[\n"
    for i, x in enumerate(v):
        pad(out, depth + 1)
        enc(out, x, depth + 1)
        if i + 1 < len(v):
            out += b","
        out += b"\n"
    pad(out, depth)
    out += b"]"


DECISIONOUTCOME_NAMES = ["permitted", "denied", "not_granted", "unknown_action", "invalid", "forbidden", "unavailable"]
DECISIONOUTCOME_UNKNOWN = "refuse"


POLICYPAGEOUTCOME_NAMES = ["page", "gap", "invalid", "forbidden", "unavailable"]
POLICYPAGEOUTCOME_UNKNOWN = "refuse"


POLICYEDITOUTCOME_NAMES = ["applied", "conflict", "invalid", "forbidden", "unavailable"]
POLICYEDITOUTCOME_UNKNOWN = "refuse"


RULEREADOUTCOME_NAMES = ["found", "expired", "unknown", "invalid", "forbidden", "unavailable"]
RULEREADOUTCOME_UNKNOWN = "refuse"


ACTIONEDITOUTCOME_NAMES = ["applied", "conflict", "unknown", "invalid", "exhausted", "forbidden", "unavailable"]
ACTIONEDITOUTCOME_UNKNOWN = "refuse"


OPERATIONS = ["register", "ask", "hold", "check", "apps", "grant", "revoke", "forget", "holds"]


KNOWN_RIGHTS = ["awake"]


REFUSAL_CODES = ["internal", "invalid_request", "caller_refused", "unknown_operation", "not_administrator", "pending", "denied", "denied_permanently", "unsupported_hold", "platform_refused", "unknown_app", "unknown_right", "not_granted", "bad_secret", "bad_token"]


RESOURCE_ACTIONS = ["abstraction.storage/content.read", "abstraction.storage/content.write", "abstraction.job/acceptance.submit", "abstraction.job/acceptance.cancel", "abstraction.config/user.replace", "abstraction.logging/history.read", "abstraction.model/lookup", "abstraction.router/inventory.read", "abstraction.router/route", "abstraction.storage/content.observe", "abstraction.storage/content.remove"]


# Existing rights request concepts. Secret/token/admin remain credential inputs
# checked by the service; request text never substitutes for native peer
# identity. This descriptor does not replace the existing handwritten line
# transport.
class Request:
    def __init__(self, **kw):
        self.op = kw.get("op", "")
        self.name = kw.get("name", "")
        self.rights = kw.get("rights", [])
        self.secret = kw.get("secret", "")
        self.token = kw.get("token", "")
        self.right = kw.get("right", "")
        self.why = kw.get("why", "")
        self.app = kw.get("app", "")
        self.admin = kw.get("admin", "")


# Own application registration fields; registered uses the existing RFC3339 time
# representation. Native App additionally carries identity.listen.Seen
# observation evidence.
class AppMetadata:
    def __init__(self, **kw):
        self.id = kw.get("id", "")
        self.name = kw.get("name", "")
        self.rights = kw.get("rights", [])
        self.registered = kw.get("registered", "")


# Own held-right fields; since uses existing RFC3339 time representation. Native
# Hold also carries Seen. The live Lease is connection-owned and cannot be
# recreated from these fields.
class HoldMetadata:
    def __init__(self, **kw):
        self.app = kw.get("app", "")
        self.name = kw.get("name", "")
        self.right = kw.get("right", "")
        self.why = kw.get("why", "")
        self.since = kw.get("since", "")


# Own response fields projected without native Seen evidence. Any nonempty
# code/error is refusal, including unknown codes. This is a common descriptor,
# not a replacement legacy Response codec or generated service client.
class ResponseMetadata:
    def __init__(self, **kw):
        self.code = kw.get("code", "")
        self.error = kw.get("error", "")
        self.secret = kw.get("secret", "")
        self.token = kw.get("token", "")
        self.expires = kw.get("expires", "")
        self.app = kw.get("app", None)
        self.right = kw.get("right", "")
        self.apps = kw.get("apps", [])
        self.holds = kw.get("holds", [])


# Account identifier and normalized absolute executable path. This serialized
# subject is an assertion by an explicitly authorized enforcement point. It
# contains no proof or verified flag and cannot authorize its own use. Direct
# decisions derive their subject from native receiving Program evidence.
class Subject:
    def __init__(self, **kw):
        self.account = kw.get("account", "")
        self.program = kw.get("program", "")


# A point-in-time policy decision. Only permitted allows an enforcement point to
# proceed. denied is an explicit exact deny; not_granted has no unexpired exact
# rule; unknown_action is outside the current catalogue. Evaluated
# permitted/denied/not_granted/unknown_action decisions carry an opaque revision
# observed with that decision. Errors carry none. No lease, token, cached
# permission lifetime or human consent is conveyed.
class Decision:
    def __init__(self, **kw):
        self.outcome = kw.get("outcome", "")
        self.policy_revision = kw.get("policy_revision", "")


# One exact policy rule. Subject is the administrative target, never caller
# proof. Account is 1..128 UTF-8 bytes; program is a normalized absolute path up
# to 4096 bytes; action is 1..128 bytes and belongs to the current catalogue;
# resource is 1..1024 bytes. All strings exclude control characters.
# permit=false is an explicit deny; revocation removes the exact rule and
# restores not_granted.
class PolicyRule:
    def __init__(self, **kw):
        self.subject = kw.get("subject", Subject())
        self.action = kw.get("action", "")
        self.resource = kw.get("resource", "")
        self.permit = kw.get("permit", False)


# Latest-policy enumeration: 1..64 requested rules and at most 256 KiB encoded
# reply, including catalogue, cursor and indentation. Catalogue is the
# configured seed plus registered actions, sorted, bounded to 64 actions of 128
# bytes. Rules are every retained rule, including expired rules the next write
# removes; ReadRule reports expiry and provenance. Page carries the exact
# durable content revision. A noncomplete page has a nonempty next cursor.
# Refusals have empty revision/catalog/rules/next and complete=false. Cursors
# are at most 256 UTF-8 bytes, bind receiving account/program and host
# epoch/revision, and return gap after change/restart/scope mismatch. Restart
# from empty cursor. No immutable multipage snapshot or historical change replay
# is promised.
class PolicyPage:
    def __init__(self, **kw):
        self.outcome = kw.get("outcome", "")
        self.revision = kw.get("revision", "")
        self.catalog = kw.get("catalog", [])
        self.rules = kw.get("rules", [])
        self.next = kw.get("next", "")
        self.complete = kw.get("complete", False)


# Applied/conflict carry the revision and optional exact current rule observed
# inside the conditional edit; absent current means no exact rule. Other
# outcomes carry no revision/current. A stale expected revision always
# conflicts, including when the desired state happens to match. No-op edits at
# the matching revision preserve it without writing. A lost reply is uncertain:
# retrying the same expected revision cannot overwrite a later edit, and may
# conflict after a successful original change. Reconcile the returned current
# state or fresh history before choosing another edit. This is optimistic
# concurrency, not an exactly-once mutation journal.
class PolicyEdit:
    def __init__(self, **kw):
        self.outcome = kw.get("outcome", "")
        self.revision = kw.get("revision", "")
        self.current = kw.get("current", None)


# One exact rule with its provenance. set_by is the subject the rights service
# established for the party that set the rule: the operator's receiving Program
# evidence, or the service's own account and executable for native edits. The
# request never supplies it. set_at is the service's UTC edit time as RFC 3339
# with milliseconds. why is the operator's reason of 0..256 UTF-8 bytes without
# control characters. expires is empty for a rule without expiry, or the UTC
# instant, in the same format, from which the rule decides nothing.
class RuleRecord:
    def __init__(self, **kw):
        self.rule = kw.get("rule", PolicyRule())
        self.set_by = kw.get("set_by", Subject())
        self.set_at = kw.get("set_at", "")
        self.why = kw.get("why", "")
        self.expires = kw.get("expires", "")


# found and expired carry the revision and the exact record. expired names a
# retained rule at or past its expiry; it decides nothing and the next policy
# write removes it. unknown carries the revision and no record. invalid,
# forbidden and unavailable carry neither.
class RuleRead:
    def __init__(self, **kw):
        self.outcome = kw.get("outcome", "")
        self.revision = kw.get("revision", "")
        self.record = kw.get("record", None)


# One registered catalogue action. Seeded actions have no entry. registered_by
# is the subject the rights service established for the registering party: the
# operator's receiving Program evidence, or the service's own account and
# executable for native registration. registered_at is the service's UTC time as
# RFC 3339 with milliseconds.
class CatalogEntry:
    def __init__(self, **kw):
        self.action = kw.get("action", "")
        self.registered_by = kw.get("registered_by", Subject())
        self.registered_at = kw.get("registered_at", "")


# applied, conflict and unknown carry the revision observed inside the
# conditional edit. applied and conflict carry the action's registration entry
# when one exists; a seeded or retired action has none. unknown means
# RetireAction named an action outside the catalogue. exhausted means the
# catalogue already holds 64 actions. invalid, exhausted, forbidden and
# unavailable carry no revision or entry. A stale expected revision always
# conflicts.
class ActionEdit:
    def __init__(self, **kw):
        self.outcome = kw.get("outcome", "")
        self.revision = kw.get("revision", "")
        self.current = kw.get("current", None)


class OAAuthorizationDecideArguments:
    def __init__(self, **kw):
        self.action = kw.get("action", "")
        self.resource = kw.get("resource", "")


class OAAuthorizationDecideForArguments:
    def __init__(self, **kw):
        self.subject = kw.get("subject", Subject())
        self.action = kw.get("action", "")
        self.resource = kw.get("resource", "")


class OAAuthorizationOperatorListPolicyArguments:
    def __init__(self, **kw):
        self.cursor = kw.get("cursor", "")
        self.limit = kw.get("limit", 0)


class OAAuthorizationOperatorSetRuleArguments:
    def __init__(self, **kw):
        self.expected_revision = kw.get("expected_revision", "")
        self.rule = kw.get("rule", PolicyRule())


class OAAuthorizationOperatorRevokeRuleArguments:
    def __init__(self, **kw):
        self.expected_revision = kw.get("expected_revision", "")
        self.subject = kw.get("subject", Subject())
        self.action = kw.get("action", "")
        self.resource = kw.get("resource", "")


class OAAuthorizationOperatorSetRuleForArguments:
    def __init__(self, **kw):
        self.expected_revision = kw.get("expected_revision", "")
        self.rule = kw.get("rule", PolicyRule())
        self.ttl_ms = kw.get("ttl_ms", 0)
        self.why = kw.get("why", "")


class OAAuthorizationOperatorReadRuleArguments:
    def __init__(self, **kw):
        self.subject = kw.get("subject", Subject())
        self.action = kw.get("action", "")
        self.resource = kw.get("resource", "")


class OAAuthorizationOperatorRegisterActionArguments:
    def __init__(self, **kw):
        self.expected_revision = kw.get("expected_revision", "")
        self.action = kw.get("action", "")


class OAAuthorizationOperatorRetireActionArguments:
    def __init__(self, **kw):
        self.expected_revision = kw.get("expected_revision", "")
        self.action = kw.get("action", "")


class OAServiceFrame:
    def __init__(self, **kw):
        self.version = kw.get("version", 0)
        self.service = kw.get("service", "")
        self.method = kw.get("method", "")
        self.arguments = kw.get("arguments", "")


class OAServiceReply:
    def __init__(self, **kw):
        self.version = kw.get("version", 0)
        self.service = kw.get("service", "")
        self.method = kw.get("method", "")
        self.ok = kw.get("ok", False)
        self.payload = kw.get("payload", "")


class OAServiceError:
    def __init__(self, **kw):
        self.code = kw.get("code", "")
        self.message = kw.get("message", "")


class OAAuthorizationDecideResult:
    def __init__(self, **kw):
        self.value = kw.get("value", Decision())


class OAAuthorizationDecideForResult:
    def __init__(self, **kw):
        self.value = kw.get("value", Decision())


class OAAuthorizationOperatorListPolicyResult:
    def __init__(self, **kw):
        self.value = kw.get("value", PolicyPage())


class OAAuthorizationOperatorSetRuleResult:
    def __init__(self, **kw):
        self.value = kw.get("value", PolicyEdit())


class OAAuthorizationOperatorRevokeRuleResult:
    def __init__(self, **kw):
        self.value = kw.get("value", PolicyEdit())


class OAAuthorizationOperatorSetRuleForResult:
    def __init__(self, **kw):
        self.value = kw.get("value", PolicyEdit())


class OAAuthorizationOperatorReadRuleResult:
    def __init__(self, **kw):
        self.value = kw.get("value", RuleRead())


class OAAuthorizationOperatorRegisterActionResult:
    def __init__(self, **kw):
        self.value = kw.get("value", ActionEdit())


class OAAuthorizationOperatorRetireActionResult:
    def __init__(self, **kw):
        self.value = kw.get("value", ActionEdit())


def enc_request(out, v, depth):
    out += b"{"
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "op")
    out += b": "
    esc(out, v.op)
    if v.name:
        out += b","
        out += b"\n"
        pad(out, depth + 1)
        esc(out, "name")
        out += b": "
        esc(out, v.name)
    if v.rights:
        out += b","
        out += b"\n"
        pad(out, depth + 1)
        esc(out, "rights")
        out += b": "
        strs(out, v.rights, depth + 1)
    if v.secret:
        out += b","
        out += b"\n"
        pad(out, depth + 1)
        esc(out, "secret")
        out += b": "
        esc(out, v.secret)
    if v.token:
        out += b","
        out += b"\n"
        pad(out, depth + 1)
        esc(out, "token")
        out += b": "
        esc(out, v.token)
    if v.right:
        out += b","
        out += b"\n"
        pad(out, depth + 1)
        esc(out, "right")
        out += b": "
        esc(out, v.right)
    if v.why:
        out += b","
        out += b"\n"
        pad(out, depth + 1)
        esc(out, "why")
        out += b": "
        esc(out, v.why)
    if v.app:
        out += b","
        out += b"\n"
        pad(out, depth + 1)
        esc(out, "app")
        out += b": "
        esc(out, v.app)
    if v.admin:
        out += b","
        out += b"\n"
        pad(out, depth + 1)
        esc(out, "admin")
        out += b": "
        esc(out, v.admin)
    out += b"\n"
    pad(out, depth)
    out += b"}"


def enc_appmetadata(out, v, depth):
    out += b"{"
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "id")
    out += b": "
    esc(out, v.id)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "name")
    out += b": "
    esc(out, v.name)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "rights")
    out += b": "
    strs(out, v.rights, depth + 1)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "registered")
    out += b": "
    esc(out, v.registered)
    out += b"\n"
    pad(out, depth)
    out += b"}"


def enc_holdmetadata(out, v, depth):
    out += b"{"
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "app")
    out += b": "
    esc(out, v.app)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "name")
    out += b": "
    esc(out, v.name)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "right")
    out += b": "
    esc(out, v.right)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "why")
    out += b": "
    esc(out, v.why)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "since")
    out += b": "
    esc(out, v.since)
    out += b"\n"
    pad(out, depth)
    out += b"}"


def enc_responsemetadata(out, v, depth):
    out += b"{"
    first = True
    if v.code:
        first = False
        out += b"\n"
        pad(out, depth + 1)
        esc(out, "code")
        out += b": "
        esc(out, v.code)
    if v.error:
        if not first:
            out += b","
        first = False
        out += b"\n"
        pad(out, depth + 1)
        esc(out, "error")
        out += b": "
        esc(out, v.error)
    if v.secret:
        if not first:
            out += b","
        first = False
        out += b"\n"
        pad(out, depth + 1)
        esc(out, "secret")
        out += b": "
        esc(out, v.secret)
    if v.token:
        if not first:
            out += b","
        first = False
        out += b"\n"
        pad(out, depth + 1)
        esc(out, "token")
        out += b": "
        esc(out, v.token)
    if v.expires:
        if not first:
            out += b","
        first = False
        out += b"\n"
        pad(out, depth + 1)
        esc(out, "expires")
        out += b": "
        esc(out, v.expires)
    if v.app is not None:
        if not first:
            out += b","
        first = False
        out += b"\n"
        pad(out, depth + 1)
        esc(out, "app")
        out += b": "
        enc_appmetadata(out, v.app, depth + 1)
    if v.right:
        if not first:
            out += b","
        first = False
        out += b"\n"
        pad(out, depth + 1)
        esc(out, "right")
        out += b": "
        esc(out, v.right)
    if v.apps:
        if not first:
            out += b","
        first = False
        out += b"\n"
        pad(out, depth + 1)
        esc(out, "apps")
        out += b": "
        enc_list(out, v.apps, depth + 1, enc_appmetadata)
    if v.holds:
        if not first:
            out += b","
        first = False
        out += b"\n"
        pad(out, depth + 1)
        esc(out, "holds")
        out += b": "
        enc_list(out, v.holds, depth + 1, enc_holdmetadata)
    if not first:
        out += b"\n"
        pad(out, depth)
    out += b"}"


def enc_subject(out, v, depth):
    out += b"{"
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "account")
    out += b": "
    esc(out, v.account)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "program")
    out += b": "
    esc(out, v.program)
    out += b"\n"
    pad(out, depth)
    out += b"}"


def enc_decision(out, v, depth):
    if type(v.outcome) is not str: raise Refusal("wrong_type",0)
    if v.outcome != "permitted" and v.outcome != "denied" and v.outcome != "not_granted" and v.outcome != "unknown_action" and v.outcome != "invalid" and v.outcome != "forbidden" and v.outcome != "unavailable": raise Refusal("bad_enum",0)
    out += b"{"
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "outcome")
    out += b": "
    esc(out, v.outcome)
    if v.policy_revision:
        out += b","
        out += b"\n"
        pad(out, depth + 1)
        esc(out, "policy_revision")
        out += b": "
        esc(out, v.policy_revision)
    out += b"\n"
    pad(out, depth)
    out += b"}"


def enc_policyrule(out, v, depth):
    out += b"{"
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "subject")
    out += b": "
    enc_subject(out, v.subject, depth + 1)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "action")
    out += b": "
    esc(out, v.action)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "resource")
    out += b": "
    esc(out, v.resource)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "permit")
    out += b": "
    out += b"true" if v.permit else b"false"
    out += b"\n"
    pad(out, depth)
    out += b"}"


def enc_policypage(out, v, depth):
    if type(v.outcome) is not str: raise Refusal("wrong_type",0)
    if v.outcome != "page" and v.outcome != "gap" and v.outcome != "invalid" and v.outcome != "forbidden" and v.outcome != "unavailable": raise Refusal("bad_enum",0)
    out += b"{"
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "outcome")
    out += b": "
    esc(out, v.outcome)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "revision")
    out += b": "
    esc(out, v.revision)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "catalog")
    out += b": "
    strs(out, v.catalog, depth + 1)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "rules")
    out += b": "
    enc_list(out, v.rules, depth + 1, enc_policyrule)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "next")
    out += b": "
    esc(out, v.next)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "complete")
    out += b": "
    out += b"true" if v.complete else b"false"
    out += b"\n"
    pad(out, depth)
    out += b"}"


def enc_policyedit(out, v, depth):
    if type(v.outcome) is not str: raise Refusal("wrong_type",0)
    if v.outcome != "applied" and v.outcome != "conflict" and v.outcome != "invalid" and v.outcome != "forbidden" and v.outcome != "unavailable": raise Refusal("bad_enum",0)
    out += b"{"
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "outcome")
    out += b": "
    esc(out, v.outcome)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "revision")
    out += b": "
    esc(out, v.revision)
    if v.current is not None:
        out += b","
        out += b"\n"
        pad(out, depth + 1)
        esc(out, "current")
        out += b": "
        enc_policyrule(out, v.current, depth + 1)
    out += b"\n"
    pad(out, depth)
    out += b"}"


def enc_rulerecord(out, v, depth):
    out += b"{"
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "rule")
    out += b": "
    enc_policyrule(out, v.rule, depth + 1)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "set_by")
    out += b": "
    enc_subject(out, v.set_by, depth + 1)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "set_at")
    out += b": "
    esc(out, v.set_at)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "why")
    out += b": "
    esc(out, v.why)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "expires")
    out += b": "
    esc(out, v.expires)
    out += b"\n"
    pad(out, depth)
    out += b"}"


def enc_ruleread(out, v, depth):
    if type(v.outcome) is not str: raise Refusal("wrong_type",0)
    if v.outcome != "found" and v.outcome != "expired" and v.outcome != "unknown" and v.outcome != "invalid" and v.outcome != "forbidden" and v.outcome != "unavailable": raise Refusal("bad_enum",0)
    out += b"{"
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "outcome")
    out += b": "
    esc(out, v.outcome)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "revision")
    out += b": "
    esc(out, v.revision)
    if v.record is not None:
        out += b","
        out += b"\n"
        pad(out, depth + 1)
        esc(out, "record")
        out += b": "
        enc_rulerecord(out, v.record, depth + 1)
    out += b"\n"
    pad(out, depth)
    out += b"}"


def enc_catalogentry(out, v, depth):
    out += b"{"
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "action")
    out += b": "
    esc(out, v.action)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "registered_by")
    out += b": "
    enc_subject(out, v.registered_by, depth + 1)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "registered_at")
    out += b": "
    esc(out, v.registered_at)
    out += b"\n"
    pad(out, depth)
    out += b"}"


def enc_actionedit(out, v, depth):
    if type(v.outcome) is not str: raise Refusal("wrong_type",0)
    if v.outcome != "applied" and v.outcome != "conflict" and v.outcome != "unknown" and v.outcome != "invalid" and v.outcome != "exhausted" and v.outcome != "forbidden" and v.outcome != "unavailable": raise Refusal("bad_enum",0)
    out += b"{"
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "outcome")
    out += b": "
    esc(out, v.outcome)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "revision")
    out += b": "
    esc(out, v.revision)
    if v.current is not None:
        out += b","
        out += b"\n"
        pad(out, depth + 1)
        esc(out, "current")
        out += b": "
        enc_catalogentry(out, v.current, depth + 1)
    out += b"\n"
    pad(out, depth)
    out += b"}"


def enc_oaauthorizationdecidearguments(out, v, depth):
    out += b"{"
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "action")
    out += b": "
    esc(out, v.action)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "resource")
    out += b": "
    esc(out, v.resource)
    out += b"\n"
    pad(out, depth)
    out += b"}"


def enc_oaauthorizationdecideforarguments(out, v, depth):
    out += b"{"
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "subject")
    out += b": "
    enc_subject(out, v.subject, depth + 1)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "action")
    out += b": "
    esc(out, v.action)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "resource")
    out += b": "
    esc(out, v.resource)
    out += b"\n"
    pad(out, depth)
    out += b"}"


def enc_oaauthorizationoperatorlistpolicyarguments(out, v, depth):
    out += b"{"
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "cursor")
    out += b": "
    esc(out, v.cursor)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "limit")
    out += b": "
    num(out, v.limit)
    out += b"\n"
    pad(out, depth)
    out += b"}"


def enc_oaauthorizationoperatorsetrulearguments(out, v, depth):
    out += b"{"
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "expected_revision")
    out += b": "
    esc(out, v.expected_revision)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "rule")
    out += b": "
    enc_policyrule(out, v.rule, depth + 1)
    out += b"\n"
    pad(out, depth)
    out += b"}"


def enc_oaauthorizationoperatorrevokerulearguments(out, v, depth):
    out += b"{"
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "expected_revision")
    out += b": "
    esc(out, v.expected_revision)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "subject")
    out += b": "
    enc_subject(out, v.subject, depth + 1)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "action")
    out += b": "
    esc(out, v.action)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "resource")
    out += b": "
    esc(out, v.resource)
    out += b"\n"
    pad(out, depth)
    out += b"}"


def enc_oaauthorizationoperatorsetruleforarguments(out, v, depth):
    out += b"{"
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "expected_revision")
    out += b": "
    esc(out, v.expected_revision)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "rule")
    out += b": "
    enc_policyrule(out, v.rule, depth + 1)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "ttl_ms")
    out += b": "
    num(out, v.ttl_ms)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "why")
    out += b": "
    esc(out, v.why)
    out += b"\n"
    pad(out, depth)
    out += b"}"


def enc_oaauthorizationoperatorreadrulearguments(out, v, depth):
    out += b"{"
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "subject")
    out += b": "
    enc_subject(out, v.subject, depth + 1)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "action")
    out += b": "
    esc(out, v.action)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "resource")
    out += b": "
    esc(out, v.resource)
    out += b"\n"
    pad(out, depth)
    out += b"}"


def enc_oaauthorizationoperatorregisteractionarguments(out, v, depth):
    out += b"{"
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "expected_revision")
    out += b": "
    esc(out, v.expected_revision)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "action")
    out += b": "
    esc(out, v.action)
    out += b"\n"
    pad(out, depth)
    out += b"}"


def enc_oaauthorizationoperatorretireactionarguments(out, v, depth):
    out += b"{"
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "expected_revision")
    out += b": "
    esc(out, v.expected_revision)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "action")
    out += b": "
    esc(out, v.action)
    out += b"\n"
    pad(out, depth)
    out += b"}"


def enc_oaserviceframe(out, v, depth):
    out += b"{"
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "version")
    out += b": "
    num(out, v.version)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "service")
    out += b": "
    esc(out, v.service)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "method")
    out += b": "
    esc(out, v.method)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "arguments")
    out += b": "
    out += v.arguments.encode("utf-8") if isinstance(v.arguments, str) else v.arguments
    out += b"\n"
    pad(out, depth)
    out += b"}"


def enc_oaservicereply(out, v, depth):
    out += b"{"
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "version")
    out += b": "
    num(out, v.version)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "service")
    out += b": "
    esc(out, v.service)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "method")
    out += b": "
    esc(out, v.method)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "ok")
    out += b": "
    out += b"true" if v.ok else b"false"
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "payload")
    out += b": "
    out += v.payload.encode("utf-8") if isinstance(v.payload, str) else v.payload
    out += b"\n"
    pad(out, depth)
    out += b"}"


def enc_oaserviceerror(out, v, depth):
    out += b"{"
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "code")
    out += b": "
    esc(out, v.code)
    out += b","
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "message")
    out += b": "
    esc(out, v.message)
    out += b"\n"
    pad(out, depth)
    out += b"}"


def enc_oaauthorizationdecideresult(out, v, depth):
    out += b"{"
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "value")
    out += b": "
    enc_decision(out, v.value, depth + 1)
    out += b"\n"
    pad(out, depth)
    out += b"}"


def enc_oaauthorizationdecideforresult(out, v, depth):
    out += b"{"
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "value")
    out += b": "
    enc_decision(out, v.value, depth + 1)
    out += b"\n"
    pad(out, depth)
    out += b"}"


def enc_oaauthorizationoperatorlistpolicyresult(out, v, depth):
    out += b"{"
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "value")
    out += b": "
    enc_policypage(out, v.value, depth + 1)
    out += b"\n"
    pad(out, depth)
    out += b"}"


def enc_oaauthorizationoperatorsetruleresult(out, v, depth):
    out += b"{"
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "value")
    out += b": "
    enc_policyedit(out, v.value, depth + 1)
    out += b"\n"
    pad(out, depth)
    out += b"}"


def enc_oaauthorizationoperatorrevokeruleresult(out, v, depth):
    out += b"{"
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "value")
    out += b": "
    enc_policyedit(out, v.value, depth + 1)
    out += b"\n"
    pad(out, depth)
    out += b"}"


def enc_oaauthorizationoperatorsetruleforresult(out, v, depth):
    out += b"{"
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "value")
    out += b": "
    enc_policyedit(out, v.value, depth + 1)
    out += b"\n"
    pad(out, depth)
    out += b"}"


def enc_oaauthorizationoperatorreadruleresult(out, v, depth):
    out += b"{"
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "value")
    out += b": "
    enc_ruleread(out, v.value, depth + 1)
    out += b"\n"
    pad(out, depth)
    out += b"}"


def enc_oaauthorizationoperatorregisteractionresult(out, v, depth):
    out += b"{"
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "value")
    out += b": "
    enc_actionedit(out, v.value, depth + 1)
    out += b"\n"
    pad(out, depth)
    out += b"}"


def enc_oaauthorizationoperatorretireactionresult(out, v, depth):
    out += b"{"
    out += b"\n"
    pad(out, depth + 1)
    esc(out, "value")
    out += b": "
    enc_actionedit(out, v.value, depth + 1)
    out += b"\n"
    pad(out, depth)
    out += b"}"


def encode(v):
    out = bytearray()
    enc_request(out, v, 0)
    out += b"\n"
    return bytes(out)


_QUOTE, _BACKSLASH, _COLON, _COMMA = 0x22, 0x5C, 0x3A, 0x2C
_LBRACE, _RBRACE, _LBRACK, _RBRACK = 0x7B, 0x7D, 0x5B, 0x5D
_MINUS, _ZERO, _NINE, _DOT, _LOWER_U, _LOWER_N = 0x2D, 0x30, 0x39, 0x2E, 0x75, 0x6E
_UNESCAPE = {0x22: 0x22, 0x5C: 0x5C, 0x2F: 0x2F, 0x62: 0x08,
             0x66: 0x0C, 0x6E: 0x0A, 0x72: 0x0D, 0x74: 0x09}
_DEPTH_LIMIT = 64
_I64_DIGITS = 19


class Refusal(ValueError):
    def __init__(self, word, offset):
        super().__init__("refused: %s at byte %d" % (word, offset))
        self.word = word
        self.offset = offset


class _Reader:
    __slots__ = ("buf", "pos", "depth")

    def __init__(self, buf):
        self.buf = buf
        self.pos = 0
        self.depth = 0

    def refuse(self, word):
        return Refusal(word, self.pos)

    def at(self):
        return self.buf[self.pos] if self.pos < len(self.buf) else 0

    def ws(self):
        while self.pos < len(self.buf) and self.buf[self.pos] in _WS:
            self.pos += 1

    def enter(self):
        self.depth += 1
        if self.depth > _DEPTH_LIMIT:
            raise self.refuse("depth_exceeded")

    def digit(self):
        return _ZERO <= self.at() <= _NINE

    def string(self):
        if self.at() != _QUOTE:
            raise self.refuse("wrong_type")
        self.pos += 1
        out = bytearray()
        while True:
            if self.pos >= len(self.buf):
                raise self.refuse("malformed")
            c = self.buf[self.pos]
            if c == _QUOTE:
                self.pos += 1
                try:
                    return out.decode("utf-8")
                except UnicodeDecodeError:
                    raise self.refuse("bad_string") from None
            if c < 0x20:
                raise self.refuse("bad_string")
            if c == _BACKSLASH:
                self.escape(out)
            else:
                out.append(c)
                self.pos += 1

    def escape(self, out):
        self.pos += 1
        if self.pos >= len(self.buf):
            raise self.refuse("malformed")
        c = self.buf[self.pos]
        self.pos += 1
        plain = _UNESCAPE.get(c)
        if plain is not None:
            out.append(plain)
            return
        if c != _LOWER_U:
            raise self.refuse("bad_string")
        u = self.hex4()
        if 0xDC00 <= u <= 0xDFFF:
            raise self.refuse("bad_string")
        if 0xD800 <= u <= 0xDBFF:
            if (self.pos + 1 >= len(self.buf) or self.buf[self.pos] != _BACKSLASH
                    or self.buf[self.pos + 1] != _LOWER_U):
                raise self.refuse("bad_string")
            self.pos += 2
            low = self.hex4()
            if not 0xDC00 <= low <= 0xDFFF:
                raise self.refuse("bad_string")
            u = 0x10000 + ((u - 0xD800) << 10) + (low - 0xDC00)
        out += chr(u).encode("utf-8")

    def hex4(self):
        if self.pos + 4 > len(self.buf):
            raise self.refuse("bad_string")
        u = 0
        for i in range(4):
            c = self.buf[self.pos + i]
            if _ZERO <= c <= _NINE:
                u = u << 4 | (c - _ZERO)
            elif 0x61 <= c <= 0x66:
                u = u << 4 | (c - 0x61 + 10)
            elif 0x41 <= c <= 0x46:
                u = u << 4 | (c - 0x41 + 10)
            else:
                raise self.refuse("bad_string")
        self.pos += 4
        return u

    def integer(self, low, high):
        c = self.at()
        if c != _MINUS and not _ZERO <= c <= _NINE:
            raise self.refuse("wrong_type")
        negative = c == _MINUS
        if negative:
            self.pos += 1
        if not self.digit():
            raise self.refuse("malformed")
        start = self.pos
        if self.at() == _ZERO:
            self.pos += 1
        else:
            while self.digit():
                self.pos += 1
        if self.at() in (_DOT, 0x65, 0x45) or self.digit():
            raise self.refuse("number_spelling")
        if self.pos - start > _I64_DIGITS:
            raise self.refuse("number_spelling")
        n = int(self.buf[start:self.pos])
        if negative:
            if n == 0:
                raise self.refuse("number_spelling")
            n = -n
        if n < low or n > high:
            raise self.refuse("number_spelling")
        return n

    def boolean(self):
        if self.at() == 0x74:
            self.literal(b"true")
            return True
        if self.at() == 0x66:
            self.literal(b"false")
            return False
        raise self.refuse("wrong_type")

    def literal(self, word):
        if self.buf[self.pos:self.pos + len(word)] != word:
            raise self.refuse("malformed")
        self.pos += len(word)

    def str_list(self):
        if self.at() != _LBRACK:
            raise self.refuse("wrong_type")
        self.enter()
        self.pos += 1
        out = []
        self.ws()
        if self.at() != _RBRACK:
            while True:
                self.ws()
                out.append(self.string())
                self.ws()
                if self.at() != _COMMA:
                    break
                self.pos += 1
        if self.at() != _RBRACK:
            raise self.refuse("malformed")
        self.pos += 1
        self.depth -= 1
        return out

    def raw_map(self):
        if self.at() != _LBRACE:
            raise self.refuse("wrong_type")
        self.enter()
        self.pos += 1
        out = {}
        self.ws()
        if self.at() != _RBRACE:
            while True:
                self.ws()
                if self.at() != _QUOTE:
                    raise self.refuse("malformed")
                k = self.string()
                if k in out:
                    raise self.refuse("duplicate_key")
                self.ws()
                if self.at() != _COLON:
                    raise self.refuse("malformed")
                self.pos += 1
                self.ws()
                out[k] = self.raw_value()
                self.ws()
                if self.at() != _COMMA:
                    break
                self.pos += 1
        if self.at() != _RBRACE:
            raise self.refuse("malformed")
        self.pos += 1
        self.depth -= 1
        return out

    def raw_value(self):
        start = self.pos
        self.skip_value()
        return self.buf[start:self.pos]

    def skip_value(self):
        c = self.at()
        if c == _QUOTE:
            self.skip_string()
        elif c in (_LBRACE, _LBRACK):
            self.skip_container()
        elif c == 0x74:
            self.literal(b"true")
        elif c == 0x66:
            self.literal(b"false")
        elif c == 0x6E:
            self.literal(b"null")
        elif c == _MINUS or _ZERO <= c <= _NINE:
            self.skip_number()
        else:
            raise self.refuse("malformed")

    # An opaque value is validated and carried, never interpreted. Its syntax is
    # the record's own — the same string reader, so an escape, a control byte or
    # a lone surrogate is judged identically at any depth — and what it is free
    # to spell, it keeps: the bytes between these two offsets are what a writer
    # re-emits. A number is walked, never converted, so a value no host type
    # holds survives the reader that carried it.
    def skip_string(self):
        self.string()

    def some_digits(self):
        n = 0
        while _ZERO <= self.at() <= _NINE:
            self.pos += 1
            n += 1
        if n == 0:
            raise self.refuse("number_spelling")

    def skip_number(self):
        if self.at() == _MINUS:
            self.pos += 1
        c = self.at()
        if c == _ZERO:
            self.pos += 1
        elif _ZERO < c <= _NINE:
            while _ZERO <= self.at() <= _NINE:
                self.pos += 1
        else:
            raise self.refuse("number_spelling")
        if self.at() == _DOT:
            self.pos += 1
            self.some_digits()
        if self.at() in (0x65, 0x45):
            self.pos += 1
            if self.at() in (0x2B, _MINUS):
                self.pos += 1
            self.some_digits()

    def skip_container(self):
        opener = self.at()
        shut = _RBRACE if opener == _LBRACE else _RBRACK
        self.enter()
        self.pos += 1
        self.ws()
        seen = set()
        if self.at() != shut:
            while True:
                self.ws()
                if opener == _LBRACE:
                    if self.at() != _QUOTE:
                        raise self.refuse("malformed")
                    k = self.string()
                    if k in seen:
                        raise self.refuse("duplicate_key")
                    seen.add(k)
                    self.ws()
                    if self.at() != _COLON:
                        raise self.refuse("malformed")
                    self.pos += 1
                    self.ws()
                self.skip_value()
                self.ws()
                if self.at() != _COMMA:
                    break
                self.pos += 1
        if self.at() != shut:
            raise self.refuse("malformed")
        self.pos += 1
        self.depth -= 1


def _decode_list(r, elem):
    if r.at() != _LBRACK:
        raise r.refuse("wrong_type")
    r.enter()
    r.pos += 1
    out = []
    r.ws()
    if r.at() != _RBRACK:
        while True:
            r.ws()
            out.append(elem(r))
            r.ws()
            if r.at() != _COMMA:
                break
            r.pos += 1
    if r.at() != _RBRACK:
        raise r.refuse("malformed")
    r.pos += 1
    r.depth -= 1
    return out


def _decode_request(r):
    if r.at() != _LBRACE:
        raise r.refuse("wrong_type")
    r.enter()
    r.pos += 1
    v = Request()
    seen = 0
    r.ws()
    if r.at() != _RBRACE:
        while True:
            r.ws()
            if r.at() != _QUOTE:
                raise r.refuse("malformed")
            key = r.string()
            r.ws()
            if r.at() != _COLON:
                raise r.refuse("malformed")
            r.pos += 1
            r.ws()
            if key == "op":
                if seen & 1:
                    raise r.refuse("duplicate_field")
                seen |= 1
                v.op = r.string()
            elif key == "name":
                if seen & 2:
                    raise r.refuse("duplicate_field")
                seen |= 2
                v.name = r.string()
            elif key == "rights":
                if seen & 4:
                    raise r.refuse("duplicate_field")
                seen |= 4
                v.rights = r.str_list()
            elif key == "secret":
                if seen & 8:
                    raise r.refuse("duplicate_field")
                seen |= 8
                v.secret = r.string()
            elif key == "token":
                if seen & 16:
                    raise r.refuse("duplicate_field")
                seen |= 16
                v.token = r.string()
            elif key == "right":
                if seen & 32:
                    raise r.refuse("duplicate_field")
                seen |= 32
                v.right = r.string()
            elif key == "why":
                if seen & 64:
                    raise r.refuse("duplicate_field")
                seen |= 64
                v.why = r.string()
            elif key == "app":
                if seen & 128:
                    raise r.refuse("duplicate_field")
                seen |= 128
                v.app = r.string()
            elif key == "admin":
                if seen & 256:
                    raise r.refuse("duplicate_field")
                seen |= 256
                v.admin = r.string()
            else:
                raise r.refuse("unknown_field")
            r.ws()
            if r.at() != _COMMA:
                break
            r.pos += 1
    if r.at() != _RBRACE:
        raise r.refuse("malformed")
    r.pos += 1
    r.depth -= 1
    if seen & 1 != 1:
        raise r.refuse("missing_field")
    return v


def _decode_appmetadata(r):
    if r.at() != _LBRACE:
        raise r.refuse("wrong_type")
    r.enter()
    r.pos += 1
    v = AppMetadata()
    seen = 0
    r.ws()
    if r.at() != _RBRACE:
        while True:
            r.ws()
            if r.at() != _QUOTE:
                raise r.refuse("malformed")
            key = r.string()
            r.ws()
            if r.at() != _COLON:
                raise r.refuse("malformed")
            r.pos += 1
            r.ws()
            if key == "id":
                if seen & 1:
                    raise r.refuse("duplicate_field")
                seen |= 1
                v.id = r.string()
            elif key == "name":
                if seen & 2:
                    raise r.refuse("duplicate_field")
                seen |= 2
                v.name = r.string()
            elif key == "rights":
                if seen & 4:
                    raise r.refuse("duplicate_field")
                seen |= 4
                v.rights = r.str_list()
            elif key == "registered":
                if seen & 8:
                    raise r.refuse("duplicate_field")
                seen |= 8
                v.registered = r.string()
            else:
                raise r.refuse("unknown_field")
            r.ws()
            if r.at() != _COMMA:
                break
            r.pos += 1
    if r.at() != _RBRACE:
        raise r.refuse("malformed")
    r.pos += 1
    r.depth -= 1
    if seen & 15 != 15:
        raise r.refuse("missing_field")
    return v


def _decode_holdmetadata(r):
    if r.at() != _LBRACE:
        raise r.refuse("wrong_type")
    r.enter()
    r.pos += 1
    v = HoldMetadata()
    seen = 0
    r.ws()
    if r.at() != _RBRACE:
        while True:
            r.ws()
            if r.at() != _QUOTE:
                raise r.refuse("malformed")
            key = r.string()
            r.ws()
            if r.at() != _COLON:
                raise r.refuse("malformed")
            r.pos += 1
            r.ws()
            if key == "app":
                if seen & 1:
                    raise r.refuse("duplicate_field")
                seen |= 1
                v.app = r.string()
            elif key == "name":
                if seen & 2:
                    raise r.refuse("duplicate_field")
                seen |= 2
                v.name = r.string()
            elif key == "right":
                if seen & 4:
                    raise r.refuse("duplicate_field")
                seen |= 4
                v.right = r.string()
            elif key == "why":
                if seen & 8:
                    raise r.refuse("duplicate_field")
                seen |= 8
                v.why = r.string()
            elif key == "since":
                if seen & 16:
                    raise r.refuse("duplicate_field")
                seen |= 16
                v.since = r.string()
            else:
                raise r.refuse("unknown_field")
            r.ws()
            if r.at() != _COMMA:
                break
            r.pos += 1
    if r.at() != _RBRACE:
        raise r.refuse("malformed")
    r.pos += 1
    r.depth -= 1
    if seen & 31 != 31:
        raise r.refuse("missing_field")
    return v


def _decode_responsemetadata(r):
    if r.at() != _LBRACE:
        raise r.refuse("wrong_type")
    r.enter()
    r.pos += 1
    v = ResponseMetadata()
    seen = 0
    r.ws()
    if r.at() != _RBRACE:
        while True:
            r.ws()
            if r.at() != _QUOTE:
                raise r.refuse("malformed")
            key = r.string()
            r.ws()
            if r.at() != _COLON:
                raise r.refuse("malformed")
            r.pos += 1
            r.ws()
            if key == "code":
                if seen & 1:
                    raise r.refuse("duplicate_field")
                seen |= 1
                v.code = r.string()
            elif key == "error":
                if seen & 2:
                    raise r.refuse("duplicate_field")
                seen |= 2
                v.error = r.string()
            elif key == "secret":
                if seen & 4:
                    raise r.refuse("duplicate_field")
                seen |= 4
                v.secret = r.string()
            elif key == "token":
                if seen & 8:
                    raise r.refuse("duplicate_field")
                seen |= 8
                v.token = r.string()
            elif key == "expires":
                if seen & 16:
                    raise r.refuse("duplicate_field")
                seen |= 16
                v.expires = r.string()
            elif key == "app":
                if seen & 32:
                    raise r.refuse("duplicate_field")
                seen |= 32
                v.app = _decode_appmetadata(r)
            elif key == "right":
                if seen & 64:
                    raise r.refuse("duplicate_field")
                seen |= 64
                v.right = r.string()
            elif key == "apps":
                if seen & 128:
                    raise r.refuse("duplicate_field")
                seen |= 128
                v.apps = _decode_list(r, _decode_appmetadata)
            elif key == "holds":
                if seen & 256:
                    raise r.refuse("duplicate_field")
                seen |= 256
                v.holds = _decode_list(r, _decode_holdmetadata)
            else:
                raise r.refuse("unknown_field")
            r.ws()
            if r.at() != _COMMA:
                break
            r.pos += 1
    if r.at() != _RBRACE:
        raise r.refuse("malformed")
    r.pos += 1
    r.depth -= 1
    return v


def _decode_subject(r):
    if r.at() != _LBRACE:
        raise r.refuse("wrong_type")
    r.enter()
    r.pos += 1
    v = Subject()
    seen = 0
    r.ws()
    if r.at() != _RBRACE:
        while True:
            r.ws()
            if r.at() != _QUOTE:
                raise r.refuse("malformed")
            key = r.string()
            r.ws()
            if r.at() != _COLON:
                raise r.refuse("malformed")
            r.pos += 1
            r.ws()
            if key == "account":
                if seen & 1:
                    raise r.refuse("duplicate_field")
                seen |= 1
                v.account = r.string()
            elif key == "program":
                if seen & 2:
                    raise r.refuse("duplicate_field")
                seen |= 2
                v.program = r.string()
            else:
                raise r.refuse("unknown_field")
            r.ws()
            if r.at() != _COMMA:
                break
            r.pos += 1
    if r.at() != _RBRACE:
        raise r.refuse("malformed")
    r.pos += 1
    r.depth -= 1
    if seen & 3 != 3:
        raise r.refuse("missing_field")
    return v


def _decode_decision(r):
    if r.at() != _LBRACE:
        raise r.refuse("wrong_type")
    r.enter()
    r.pos += 1
    v = Decision()
    seen = 0
    r.ws()
    if r.at() != _RBRACE:
        while True:
            r.ws()
            if r.at() != _QUOTE:
                raise r.refuse("malformed")
            key = r.string()
            r.ws()
            if r.at() != _COLON:
                raise r.refuse("malformed")
            r.pos += 1
            r.ws()
            if key == "outcome":
                if seen & 1:
                    raise r.refuse("duplicate_field")
                seen |= 1
                v.outcome = r.string()
            elif key == "policy_revision":
                if seen & 2:
                    raise r.refuse("duplicate_field")
                seen |= 2
                v.policy_revision = r.string()
            else:
                raise r.refuse("unknown_field")
            r.ws()
            if r.at() != _COMMA:
                break
            r.pos += 1
    if r.at() != _RBRACE:
        raise r.refuse("malformed")
    r.pos += 1
    r.depth -= 1
    if seen & 1 != 1:
        raise r.refuse("missing_field")
    if v.outcome != "permitted" and v.outcome != "denied" and v.outcome != "not_granted" and v.outcome != "unknown_action" and v.outcome != "invalid" and v.outcome != "forbidden" and v.outcome != "unavailable": raise r.refuse("bad_enum")
    return v


def _decode_policyrule(r):
    if r.at() != _LBRACE:
        raise r.refuse("wrong_type")
    r.enter()
    r.pos += 1
    v = PolicyRule()
    seen = 0
    r.ws()
    if r.at() != _RBRACE:
        while True:
            r.ws()
            if r.at() != _QUOTE:
                raise r.refuse("malformed")
            key = r.string()
            r.ws()
            if r.at() != _COLON:
                raise r.refuse("malformed")
            r.pos += 1
            r.ws()
            if key == "subject":
                if seen & 1:
                    raise r.refuse("duplicate_field")
                seen |= 1
                v.subject = _decode_subject(r)
            elif key == "action":
                if seen & 2:
                    raise r.refuse("duplicate_field")
                seen |= 2
                v.action = r.string()
            elif key == "resource":
                if seen & 4:
                    raise r.refuse("duplicate_field")
                seen |= 4
                v.resource = r.string()
            elif key == "permit":
                if seen & 8:
                    raise r.refuse("duplicate_field")
                seen |= 8
                v.permit = r.boolean()
            else:
                raise r.refuse("unknown_field")
            r.ws()
            if r.at() != _COMMA:
                break
            r.pos += 1
    if r.at() != _RBRACE:
        raise r.refuse("malformed")
    r.pos += 1
    r.depth -= 1
    if seen & 15 != 15:
        raise r.refuse("missing_field")
    return v


def _decode_policypage(r):
    if r.at() != _LBRACE:
        raise r.refuse("wrong_type")
    r.enter()
    r.pos += 1
    v = PolicyPage()
    seen = 0
    r.ws()
    if r.at() != _RBRACE:
        while True:
            r.ws()
            if r.at() != _QUOTE:
                raise r.refuse("malformed")
            key = r.string()
            r.ws()
            if r.at() != _COLON:
                raise r.refuse("malformed")
            r.pos += 1
            r.ws()
            if key == "outcome":
                if seen & 1:
                    raise r.refuse("duplicate_field")
                seen |= 1
                v.outcome = r.string()
            elif key == "revision":
                if seen & 2:
                    raise r.refuse("duplicate_field")
                seen |= 2
                v.revision = r.string()
            elif key == "catalog":
                if seen & 4:
                    raise r.refuse("duplicate_field")
                seen |= 4
                v.catalog = r.str_list()
            elif key == "rules":
                if seen & 8:
                    raise r.refuse("duplicate_field")
                seen |= 8
                v.rules = _decode_list(r, _decode_policyrule)
            elif key == "next":
                if seen & 16:
                    raise r.refuse("duplicate_field")
                seen |= 16
                v.next = r.string()
            elif key == "complete":
                if seen & 32:
                    raise r.refuse("duplicate_field")
                seen |= 32
                v.complete = r.boolean()
            else:
                raise r.refuse("unknown_field")
            r.ws()
            if r.at() != _COMMA:
                break
            r.pos += 1
    if r.at() != _RBRACE:
        raise r.refuse("malformed")
    r.pos += 1
    r.depth -= 1
    if seen & 63 != 63:
        raise r.refuse("missing_field")
    if v.outcome != "page" and v.outcome != "gap" and v.outcome != "invalid" and v.outcome != "forbidden" and v.outcome != "unavailable": raise r.refuse("bad_enum")
    return v


def _decode_policyedit(r):
    if r.at() != _LBRACE:
        raise r.refuse("wrong_type")
    r.enter()
    r.pos += 1
    v = PolicyEdit()
    seen = 0
    r.ws()
    if r.at() != _RBRACE:
        while True:
            r.ws()
            if r.at() != _QUOTE:
                raise r.refuse("malformed")
            key = r.string()
            r.ws()
            if r.at() != _COLON:
                raise r.refuse("malformed")
            r.pos += 1
            r.ws()
            if key == "outcome":
                if seen & 1:
                    raise r.refuse("duplicate_field")
                seen |= 1
                v.outcome = r.string()
            elif key == "revision":
                if seen & 2:
                    raise r.refuse("duplicate_field")
                seen |= 2
                v.revision = r.string()
            elif key == "current":
                if seen & 4:
                    raise r.refuse("duplicate_field")
                seen |= 4
                v.current = _decode_policyrule(r)
            else:
                raise r.refuse("unknown_field")
            r.ws()
            if r.at() != _COMMA:
                break
            r.pos += 1
    if r.at() != _RBRACE:
        raise r.refuse("malformed")
    r.pos += 1
    r.depth -= 1
    if seen & 3 != 3:
        raise r.refuse("missing_field")
    if v.outcome != "applied" and v.outcome != "conflict" and v.outcome != "invalid" and v.outcome != "forbidden" and v.outcome != "unavailable": raise r.refuse("bad_enum")
    return v


def _decode_rulerecord(r):
    if r.at() != _LBRACE:
        raise r.refuse("wrong_type")
    r.enter()
    r.pos += 1
    v = RuleRecord()
    seen = 0
    r.ws()
    if r.at() != _RBRACE:
        while True:
            r.ws()
            if r.at() != _QUOTE:
                raise r.refuse("malformed")
            key = r.string()
            r.ws()
            if r.at() != _COLON:
                raise r.refuse("malformed")
            r.pos += 1
            r.ws()
            if key == "rule":
                if seen & 1:
                    raise r.refuse("duplicate_field")
                seen |= 1
                v.rule = _decode_policyrule(r)
            elif key == "set_by":
                if seen & 2:
                    raise r.refuse("duplicate_field")
                seen |= 2
                v.set_by = _decode_subject(r)
            elif key == "set_at":
                if seen & 4:
                    raise r.refuse("duplicate_field")
                seen |= 4
                v.set_at = r.string()
            elif key == "why":
                if seen & 8:
                    raise r.refuse("duplicate_field")
                seen |= 8
                v.why = r.string()
            elif key == "expires":
                if seen & 16:
                    raise r.refuse("duplicate_field")
                seen |= 16
                v.expires = r.string()
            else:
                raise r.refuse("unknown_field")
            r.ws()
            if r.at() != _COMMA:
                break
            r.pos += 1
    if r.at() != _RBRACE:
        raise r.refuse("malformed")
    r.pos += 1
    r.depth -= 1
    if seen & 31 != 31:
        raise r.refuse("missing_field")
    return v


def _decode_ruleread(r):
    if r.at() != _LBRACE:
        raise r.refuse("wrong_type")
    r.enter()
    r.pos += 1
    v = RuleRead()
    seen = 0
    r.ws()
    if r.at() != _RBRACE:
        while True:
            r.ws()
            if r.at() != _QUOTE:
                raise r.refuse("malformed")
            key = r.string()
            r.ws()
            if r.at() != _COLON:
                raise r.refuse("malformed")
            r.pos += 1
            r.ws()
            if key == "outcome":
                if seen & 1:
                    raise r.refuse("duplicate_field")
                seen |= 1
                v.outcome = r.string()
            elif key == "revision":
                if seen & 2:
                    raise r.refuse("duplicate_field")
                seen |= 2
                v.revision = r.string()
            elif key == "record":
                if seen & 4:
                    raise r.refuse("duplicate_field")
                seen |= 4
                v.record = _decode_rulerecord(r)
            else:
                raise r.refuse("unknown_field")
            r.ws()
            if r.at() != _COMMA:
                break
            r.pos += 1
    if r.at() != _RBRACE:
        raise r.refuse("malformed")
    r.pos += 1
    r.depth -= 1
    if seen & 3 != 3:
        raise r.refuse("missing_field")
    if v.outcome != "found" and v.outcome != "expired" and v.outcome != "unknown" and v.outcome != "invalid" and v.outcome != "forbidden" and v.outcome != "unavailable": raise r.refuse("bad_enum")
    return v


def _decode_catalogentry(r):
    if r.at() != _LBRACE:
        raise r.refuse("wrong_type")
    r.enter()
    r.pos += 1
    v = CatalogEntry()
    seen = 0
    r.ws()
    if r.at() != _RBRACE:
        while True:
            r.ws()
            if r.at() != _QUOTE:
                raise r.refuse("malformed")
            key = r.string()
            r.ws()
            if r.at() != _COLON:
                raise r.refuse("malformed")
            r.pos += 1
            r.ws()
            if key == "action":
                if seen & 1:
                    raise r.refuse("duplicate_field")
                seen |= 1
                v.action = r.string()
            elif key == "registered_by":
                if seen & 2:
                    raise r.refuse("duplicate_field")
                seen |= 2
                v.registered_by = _decode_subject(r)
            elif key == "registered_at":
                if seen & 4:
                    raise r.refuse("duplicate_field")
                seen |= 4
                v.registered_at = r.string()
            else:
                raise r.refuse("unknown_field")
            r.ws()
            if r.at() != _COMMA:
                break
            r.pos += 1
    if r.at() != _RBRACE:
        raise r.refuse("malformed")
    r.pos += 1
    r.depth -= 1
    if seen & 7 != 7:
        raise r.refuse("missing_field")
    return v


def _decode_actionedit(r):
    if r.at() != _LBRACE:
        raise r.refuse("wrong_type")
    r.enter()
    r.pos += 1
    v = ActionEdit()
    seen = 0
    r.ws()
    if r.at() != _RBRACE:
        while True:
            r.ws()
            if r.at() != _QUOTE:
                raise r.refuse("malformed")
            key = r.string()
            r.ws()
            if r.at() != _COLON:
                raise r.refuse("malformed")
            r.pos += 1
            r.ws()
            if key == "outcome":
                if seen & 1:
                    raise r.refuse("duplicate_field")
                seen |= 1
                v.outcome = r.string()
            elif key == "revision":
                if seen & 2:
                    raise r.refuse("duplicate_field")
                seen |= 2
                v.revision = r.string()
            elif key == "current":
                if seen & 4:
                    raise r.refuse("duplicate_field")
                seen |= 4
                v.current = _decode_catalogentry(r)
            else:
                raise r.refuse("unknown_field")
            r.ws()
            if r.at() != _COMMA:
                break
            r.pos += 1
    if r.at() != _RBRACE:
        raise r.refuse("malformed")
    r.pos += 1
    r.depth -= 1
    if seen & 3 != 3:
        raise r.refuse("missing_field")
    if v.outcome != "applied" and v.outcome != "conflict" and v.outcome != "unknown" and v.outcome != "invalid" and v.outcome != "exhausted" and v.outcome != "forbidden" and v.outcome != "unavailable": raise r.refuse("bad_enum")
    return v


def _decode_oaauthorizationdecidearguments(r):
    if r.at() != _LBRACE:
        raise r.refuse("wrong_type")
    r.enter()
    r.pos += 1
    v = OAAuthorizationDecideArguments()
    seen = 0
    r.ws()
    if r.at() != _RBRACE:
        while True:
            r.ws()
            if r.at() != _QUOTE:
                raise r.refuse("malformed")
            key = r.string()
            r.ws()
            if r.at() != _COLON:
                raise r.refuse("malformed")
            r.pos += 1
            r.ws()
            if key == "action":
                if seen & 1:
                    raise r.refuse("duplicate_field")
                seen |= 1
                v.action = r.string()
            elif key == "resource":
                if seen & 2:
                    raise r.refuse("duplicate_field")
                seen |= 2
                v.resource = r.string()
            else:
                raise r.refuse("unknown_field")
            r.ws()
            if r.at() != _COMMA:
                break
            r.pos += 1
    if r.at() != _RBRACE:
        raise r.refuse("malformed")
    r.pos += 1
    r.depth -= 1
    if seen & 3 != 3:
        raise r.refuse("missing_field")
    return v


def _decode_oaauthorizationdecideforarguments(r):
    if r.at() != _LBRACE:
        raise r.refuse("wrong_type")
    r.enter()
    r.pos += 1
    v = OAAuthorizationDecideForArguments()
    seen = 0
    r.ws()
    if r.at() != _RBRACE:
        while True:
            r.ws()
            if r.at() != _QUOTE:
                raise r.refuse("malformed")
            key = r.string()
            r.ws()
            if r.at() != _COLON:
                raise r.refuse("malformed")
            r.pos += 1
            r.ws()
            if key == "subject":
                if seen & 1:
                    raise r.refuse("duplicate_field")
                seen |= 1
                v.subject = _decode_subject(r)
            elif key == "action":
                if seen & 2:
                    raise r.refuse("duplicate_field")
                seen |= 2
                v.action = r.string()
            elif key == "resource":
                if seen & 4:
                    raise r.refuse("duplicate_field")
                seen |= 4
                v.resource = r.string()
            else:
                raise r.refuse("unknown_field")
            r.ws()
            if r.at() != _COMMA:
                break
            r.pos += 1
    if r.at() != _RBRACE:
        raise r.refuse("malformed")
    r.pos += 1
    r.depth -= 1
    if seen & 7 != 7:
        raise r.refuse("missing_field")
    return v


def _decode_oaauthorizationoperatorlistpolicyarguments(r):
    if r.at() != _LBRACE:
        raise r.refuse("wrong_type")
    r.enter()
    r.pos += 1
    v = OAAuthorizationOperatorListPolicyArguments()
    seen = 0
    r.ws()
    if r.at() != _RBRACE:
        while True:
            r.ws()
            if r.at() != _QUOTE:
                raise r.refuse("malformed")
            key = r.string()
            r.ws()
            if r.at() != _COLON:
                raise r.refuse("malformed")
            r.pos += 1
            r.ws()
            if key == "cursor":
                if seen & 1:
                    raise r.refuse("duplicate_field")
                seen |= 1
                v.cursor = r.string()
            elif key == "limit":
                if seen & 2:
                    raise r.refuse("duplicate_field")
                seen |= 2
                v.limit = r.integer(-9223372036854775808, 9223372036854775807)
            else:
                raise r.refuse("unknown_field")
            r.ws()
            if r.at() != _COMMA:
                break
            r.pos += 1
    if r.at() != _RBRACE:
        raise r.refuse("malformed")
    r.pos += 1
    r.depth -= 1
    if seen & 3 != 3:
        raise r.refuse("missing_field")
    return v


def _decode_oaauthorizationoperatorsetrulearguments(r):
    if r.at() != _LBRACE:
        raise r.refuse("wrong_type")
    r.enter()
    r.pos += 1
    v = OAAuthorizationOperatorSetRuleArguments()
    seen = 0
    r.ws()
    if r.at() != _RBRACE:
        while True:
            r.ws()
            if r.at() != _QUOTE:
                raise r.refuse("malformed")
            key = r.string()
            r.ws()
            if r.at() != _COLON:
                raise r.refuse("malformed")
            r.pos += 1
            r.ws()
            if key == "expected_revision":
                if seen & 1:
                    raise r.refuse("duplicate_field")
                seen |= 1
                v.expected_revision = r.string()
            elif key == "rule":
                if seen & 2:
                    raise r.refuse("duplicate_field")
                seen |= 2
                v.rule = _decode_policyrule(r)
            else:
                raise r.refuse("unknown_field")
            r.ws()
            if r.at() != _COMMA:
                break
            r.pos += 1
    if r.at() != _RBRACE:
        raise r.refuse("malformed")
    r.pos += 1
    r.depth -= 1
    if seen & 3 != 3:
        raise r.refuse("missing_field")
    return v


def _decode_oaauthorizationoperatorrevokerulearguments(r):
    if r.at() != _LBRACE:
        raise r.refuse("wrong_type")
    r.enter()
    r.pos += 1
    v = OAAuthorizationOperatorRevokeRuleArguments()
    seen = 0
    r.ws()
    if r.at() != _RBRACE:
        while True:
            r.ws()
            if r.at() != _QUOTE:
                raise r.refuse("malformed")
            key = r.string()
            r.ws()
            if r.at() != _COLON:
                raise r.refuse("malformed")
            r.pos += 1
            r.ws()
            if key == "expected_revision":
                if seen & 1:
                    raise r.refuse("duplicate_field")
                seen |= 1
                v.expected_revision = r.string()
            elif key == "subject":
                if seen & 2:
                    raise r.refuse("duplicate_field")
                seen |= 2
                v.subject = _decode_subject(r)
            elif key == "action":
                if seen & 4:
                    raise r.refuse("duplicate_field")
                seen |= 4
                v.action = r.string()
            elif key == "resource":
                if seen & 8:
                    raise r.refuse("duplicate_field")
                seen |= 8
                v.resource = r.string()
            else:
                raise r.refuse("unknown_field")
            r.ws()
            if r.at() != _COMMA:
                break
            r.pos += 1
    if r.at() != _RBRACE:
        raise r.refuse("malformed")
    r.pos += 1
    r.depth -= 1
    if seen & 15 != 15:
        raise r.refuse("missing_field")
    return v


def _decode_oaauthorizationoperatorsetruleforarguments(r):
    if r.at() != _LBRACE:
        raise r.refuse("wrong_type")
    r.enter()
    r.pos += 1
    v = OAAuthorizationOperatorSetRuleForArguments()
    seen = 0
    r.ws()
    if r.at() != _RBRACE:
        while True:
            r.ws()
            if r.at() != _QUOTE:
                raise r.refuse("malformed")
            key = r.string()
            r.ws()
            if r.at() != _COLON:
                raise r.refuse("malformed")
            r.pos += 1
            r.ws()
            if key == "expected_revision":
                if seen & 1:
                    raise r.refuse("duplicate_field")
                seen |= 1
                v.expected_revision = r.string()
            elif key == "rule":
                if seen & 2:
                    raise r.refuse("duplicate_field")
                seen |= 2
                v.rule = _decode_policyrule(r)
            elif key == "ttl_ms":
                if seen & 4:
                    raise r.refuse("duplicate_field")
                seen |= 4
                v.ttl_ms = r.integer(-9223372036854775808, 9223372036854775807)
            elif key == "why":
                if seen & 8:
                    raise r.refuse("duplicate_field")
                seen |= 8
                v.why = r.string()
            else:
                raise r.refuse("unknown_field")
            r.ws()
            if r.at() != _COMMA:
                break
            r.pos += 1
    if r.at() != _RBRACE:
        raise r.refuse("malformed")
    r.pos += 1
    r.depth -= 1
    if seen & 15 != 15:
        raise r.refuse("missing_field")
    return v


def _decode_oaauthorizationoperatorreadrulearguments(r):
    if r.at() != _LBRACE:
        raise r.refuse("wrong_type")
    r.enter()
    r.pos += 1
    v = OAAuthorizationOperatorReadRuleArguments()
    seen = 0
    r.ws()
    if r.at() != _RBRACE:
        while True:
            r.ws()
            if r.at() != _QUOTE:
                raise r.refuse("malformed")
            key = r.string()
            r.ws()
            if r.at() != _COLON:
                raise r.refuse("malformed")
            r.pos += 1
            r.ws()
            if key == "subject":
                if seen & 1:
                    raise r.refuse("duplicate_field")
                seen |= 1
                v.subject = _decode_subject(r)
            elif key == "action":
                if seen & 2:
                    raise r.refuse("duplicate_field")
                seen |= 2
                v.action = r.string()
            elif key == "resource":
                if seen & 4:
                    raise r.refuse("duplicate_field")
                seen |= 4
                v.resource = r.string()
            else:
                raise r.refuse("unknown_field")
            r.ws()
            if r.at() != _COMMA:
                break
            r.pos += 1
    if r.at() != _RBRACE:
        raise r.refuse("malformed")
    r.pos += 1
    r.depth -= 1
    if seen & 7 != 7:
        raise r.refuse("missing_field")
    return v


def _decode_oaauthorizationoperatorregisteractionarguments(r):
    if r.at() != _LBRACE:
        raise r.refuse("wrong_type")
    r.enter()
    r.pos += 1
    v = OAAuthorizationOperatorRegisterActionArguments()
    seen = 0
    r.ws()
    if r.at() != _RBRACE:
        while True:
            r.ws()
            if r.at() != _QUOTE:
                raise r.refuse("malformed")
            key = r.string()
            r.ws()
            if r.at() != _COLON:
                raise r.refuse("malformed")
            r.pos += 1
            r.ws()
            if key == "expected_revision":
                if seen & 1:
                    raise r.refuse("duplicate_field")
                seen |= 1
                v.expected_revision = r.string()
            elif key == "action":
                if seen & 2:
                    raise r.refuse("duplicate_field")
                seen |= 2
                v.action = r.string()
            else:
                raise r.refuse("unknown_field")
            r.ws()
            if r.at() != _COMMA:
                break
            r.pos += 1
    if r.at() != _RBRACE:
        raise r.refuse("malformed")
    r.pos += 1
    r.depth -= 1
    if seen & 3 != 3:
        raise r.refuse("missing_field")
    return v


def _decode_oaauthorizationoperatorretireactionarguments(r):
    if r.at() != _LBRACE:
        raise r.refuse("wrong_type")
    r.enter()
    r.pos += 1
    v = OAAuthorizationOperatorRetireActionArguments()
    seen = 0
    r.ws()
    if r.at() != _RBRACE:
        while True:
            r.ws()
            if r.at() != _QUOTE:
                raise r.refuse("malformed")
            key = r.string()
            r.ws()
            if r.at() != _COLON:
                raise r.refuse("malformed")
            r.pos += 1
            r.ws()
            if key == "expected_revision":
                if seen & 1:
                    raise r.refuse("duplicate_field")
                seen |= 1
                v.expected_revision = r.string()
            elif key == "action":
                if seen & 2:
                    raise r.refuse("duplicate_field")
                seen |= 2
                v.action = r.string()
            else:
                raise r.refuse("unknown_field")
            r.ws()
            if r.at() != _COMMA:
                break
            r.pos += 1
    if r.at() != _RBRACE:
        raise r.refuse("malformed")
    r.pos += 1
    r.depth -= 1
    if seen & 3 != 3:
        raise r.refuse("missing_field")
    return v


def _decode_oaserviceframe(r):
    if r.at() != _LBRACE:
        raise r.refuse("wrong_type")
    r.enter()
    r.pos += 1
    v = OAServiceFrame()
    seen = 0
    r.ws()
    if r.at() != _RBRACE:
        while True:
            r.ws()
            if r.at() != _QUOTE:
                raise r.refuse("malformed")
            key = r.string()
            r.ws()
            if r.at() != _COLON:
                raise r.refuse("malformed")
            r.pos += 1
            r.ws()
            if key == "version":
                if seen & 1:
                    raise r.refuse("duplicate_field")
                seen |= 1
                v.version = r.integer(-2147483648, 2147483647)
            elif key == "service":
                if seen & 2:
                    raise r.refuse("duplicate_field")
                seen |= 2
                v.service = r.string()
            elif key == "method":
                if seen & 4:
                    raise r.refuse("duplicate_field")
                seen |= 4
                v.method = r.string()
            elif key == "arguments":
                if seen & 8:
                    raise r.refuse("duplicate_field")
                seen |= 8
                v.arguments = r.raw_value()
            else:
                raise r.refuse("unknown_field")
            r.ws()
            if r.at() != _COMMA:
                break
            r.pos += 1
    if r.at() != _RBRACE:
        raise r.refuse("malformed")
    r.pos += 1
    r.depth -= 1
    if seen & 15 != 15:
        raise r.refuse("missing_field")
    return v


def _decode_oaservicereply(r):
    if r.at() != _LBRACE:
        raise r.refuse("wrong_type")
    r.enter()
    r.pos += 1
    v = OAServiceReply()
    seen = 0
    r.ws()
    if r.at() != _RBRACE:
        while True:
            r.ws()
            if r.at() != _QUOTE:
                raise r.refuse("malformed")
            key = r.string()
            r.ws()
            if r.at() != _COLON:
                raise r.refuse("malformed")
            r.pos += 1
            r.ws()
            if key == "version":
                if seen & 1:
                    raise r.refuse("duplicate_field")
                seen |= 1
                v.version = r.integer(-2147483648, 2147483647)
            elif key == "service":
                if seen & 2:
                    raise r.refuse("duplicate_field")
                seen |= 2
                v.service = r.string()
            elif key == "method":
                if seen & 4:
                    raise r.refuse("duplicate_field")
                seen |= 4
                v.method = r.string()
            elif key == "ok":
                if seen & 8:
                    raise r.refuse("duplicate_field")
                seen |= 8
                v.ok = r.boolean()
            elif key == "payload":
                if seen & 16:
                    raise r.refuse("duplicate_field")
                seen |= 16
                v.payload = r.raw_value()
            else:
                raise r.refuse("unknown_field")
            r.ws()
            if r.at() != _COMMA:
                break
            r.pos += 1
    if r.at() != _RBRACE:
        raise r.refuse("malformed")
    r.pos += 1
    r.depth -= 1
    if seen & 31 != 31:
        raise r.refuse("missing_field")
    return v


def _decode_oaserviceerror(r):
    if r.at() != _LBRACE:
        raise r.refuse("wrong_type")
    r.enter()
    r.pos += 1
    v = OAServiceError()
    seen = 0
    r.ws()
    if r.at() != _RBRACE:
        while True:
            r.ws()
            if r.at() != _QUOTE:
                raise r.refuse("malformed")
            key = r.string()
            r.ws()
            if r.at() != _COLON:
                raise r.refuse("malformed")
            r.pos += 1
            r.ws()
            if key == "code":
                if seen & 1:
                    raise r.refuse("duplicate_field")
                seen |= 1
                v.code = r.string()
            elif key == "message":
                if seen & 2:
                    raise r.refuse("duplicate_field")
                seen |= 2
                v.message = r.string()
            else:
                raise r.refuse("unknown_field")
            r.ws()
            if r.at() != _COMMA:
                break
            r.pos += 1
    if r.at() != _RBRACE:
        raise r.refuse("malformed")
    r.pos += 1
    r.depth -= 1
    if seen & 3 != 3:
        raise r.refuse("missing_field")
    return v


def _decode_oaauthorizationdecideresult(r):
    if r.at() != _LBRACE:
        raise r.refuse("wrong_type")
    r.enter()
    r.pos += 1
    v = OAAuthorizationDecideResult()
    seen = 0
    r.ws()
    if r.at() != _RBRACE:
        while True:
            r.ws()
            if r.at() != _QUOTE:
                raise r.refuse("malformed")
            key = r.string()
            r.ws()
            if r.at() != _COLON:
                raise r.refuse("malformed")
            r.pos += 1
            r.ws()
            if key == "value":
                if seen & 1:
                    raise r.refuse("duplicate_field")
                seen |= 1
                v.value = _decode_decision(r)
            else:
                raise r.refuse("unknown_field")
            r.ws()
            if r.at() != _COMMA:
                break
            r.pos += 1
    if r.at() != _RBRACE:
        raise r.refuse("malformed")
    r.pos += 1
    r.depth -= 1
    if seen & 1 != 1:
        raise r.refuse("missing_field")
    return v


def _decode_oaauthorizationdecideforresult(r):
    if r.at() != _LBRACE:
        raise r.refuse("wrong_type")
    r.enter()
    r.pos += 1
    v = OAAuthorizationDecideForResult()
    seen = 0
    r.ws()
    if r.at() != _RBRACE:
        while True:
            r.ws()
            if r.at() != _QUOTE:
                raise r.refuse("malformed")
            key = r.string()
            r.ws()
            if r.at() != _COLON:
                raise r.refuse("malformed")
            r.pos += 1
            r.ws()
            if key == "value":
                if seen & 1:
                    raise r.refuse("duplicate_field")
                seen |= 1
                v.value = _decode_decision(r)
            else:
                raise r.refuse("unknown_field")
            r.ws()
            if r.at() != _COMMA:
                break
            r.pos += 1
    if r.at() != _RBRACE:
        raise r.refuse("malformed")
    r.pos += 1
    r.depth -= 1
    if seen & 1 != 1:
        raise r.refuse("missing_field")
    return v


def _decode_oaauthorizationoperatorlistpolicyresult(r):
    if r.at() != _LBRACE:
        raise r.refuse("wrong_type")
    r.enter()
    r.pos += 1
    v = OAAuthorizationOperatorListPolicyResult()
    seen = 0
    r.ws()
    if r.at() != _RBRACE:
        while True:
            r.ws()
            if r.at() != _QUOTE:
                raise r.refuse("malformed")
            key = r.string()
            r.ws()
            if r.at() != _COLON:
                raise r.refuse("malformed")
            r.pos += 1
            r.ws()
            if key == "value":
                if seen & 1:
                    raise r.refuse("duplicate_field")
                seen |= 1
                v.value = _decode_policypage(r)
            else:
                raise r.refuse("unknown_field")
            r.ws()
            if r.at() != _COMMA:
                break
            r.pos += 1
    if r.at() != _RBRACE:
        raise r.refuse("malformed")
    r.pos += 1
    r.depth -= 1
    if seen & 1 != 1:
        raise r.refuse("missing_field")
    return v


def _decode_oaauthorizationoperatorsetruleresult(r):
    if r.at() != _LBRACE:
        raise r.refuse("wrong_type")
    r.enter()
    r.pos += 1
    v = OAAuthorizationOperatorSetRuleResult()
    seen = 0
    r.ws()
    if r.at() != _RBRACE:
        while True:
            r.ws()
            if r.at() != _QUOTE:
                raise r.refuse("malformed")
            key = r.string()
            r.ws()
            if r.at() != _COLON:
                raise r.refuse("malformed")
            r.pos += 1
            r.ws()
            if key == "value":
                if seen & 1:
                    raise r.refuse("duplicate_field")
                seen |= 1
                v.value = _decode_policyedit(r)
            else:
                raise r.refuse("unknown_field")
            r.ws()
            if r.at() != _COMMA:
                break
            r.pos += 1
    if r.at() != _RBRACE:
        raise r.refuse("malformed")
    r.pos += 1
    r.depth -= 1
    if seen & 1 != 1:
        raise r.refuse("missing_field")
    return v


def _decode_oaauthorizationoperatorrevokeruleresult(r):
    if r.at() != _LBRACE:
        raise r.refuse("wrong_type")
    r.enter()
    r.pos += 1
    v = OAAuthorizationOperatorRevokeRuleResult()
    seen = 0
    r.ws()
    if r.at() != _RBRACE:
        while True:
            r.ws()
            if r.at() != _QUOTE:
                raise r.refuse("malformed")
            key = r.string()
            r.ws()
            if r.at() != _COLON:
                raise r.refuse("malformed")
            r.pos += 1
            r.ws()
            if key == "value":
                if seen & 1:
                    raise r.refuse("duplicate_field")
                seen |= 1
                v.value = _decode_policyedit(r)
            else:
                raise r.refuse("unknown_field")
            r.ws()
            if r.at() != _COMMA:
                break
            r.pos += 1
    if r.at() != _RBRACE:
        raise r.refuse("malformed")
    r.pos += 1
    r.depth -= 1
    if seen & 1 != 1:
        raise r.refuse("missing_field")
    return v


def _decode_oaauthorizationoperatorsetruleforresult(r):
    if r.at() != _LBRACE:
        raise r.refuse("wrong_type")
    r.enter()
    r.pos += 1
    v = OAAuthorizationOperatorSetRuleForResult()
    seen = 0
    r.ws()
    if r.at() != _RBRACE:
        while True:
            r.ws()
            if r.at() != _QUOTE:
                raise r.refuse("malformed")
            key = r.string()
            r.ws()
            if r.at() != _COLON:
                raise r.refuse("malformed")
            r.pos += 1
            r.ws()
            if key == "value":
                if seen & 1:
                    raise r.refuse("duplicate_field")
                seen |= 1
                v.value = _decode_policyedit(r)
            else:
                raise r.refuse("unknown_field")
            r.ws()
            if r.at() != _COMMA:
                break
            r.pos += 1
    if r.at() != _RBRACE:
        raise r.refuse("malformed")
    r.pos += 1
    r.depth -= 1
    if seen & 1 != 1:
        raise r.refuse("missing_field")
    return v


def _decode_oaauthorizationoperatorreadruleresult(r):
    if r.at() != _LBRACE:
        raise r.refuse("wrong_type")
    r.enter()
    r.pos += 1
    v = OAAuthorizationOperatorReadRuleResult()
    seen = 0
    r.ws()
    if r.at() != _RBRACE:
        while True:
            r.ws()
            if r.at() != _QUOTE:
                raise r.refuse("malformed")
            key = r.string()
            r.ws()
            if r.at() != _COLON:
                raise r.refuse("malformed")
            r.pos += 1
            r.ws()
            if key == "value":
                if seen & 1:
                    raise r.refuse("duplicate_field")
                seen |= 1
                v.value = _decode_ruleread(r)
            else:
                raise r.refuse("unknown_field")
            r.ws()
            if r.at() != _COMMA:
                break
            r.pos += 1
    if r.at() != _RBRACE:
        raise r.refuse("malformed")
    r.pos += 1
    r.depth -= 1
    if seen & 1 != 1:
        raise r.refuse("missing_field")
    return v


def _decode_oaauthorizationoperatorregisteractionresult(r):
    if r.at() != _LBRACE:
        raise r.refuse("wrong_type")
    r.enter()
    r.pos += 1
    v = OAAuthorizationOperatorRegisterActionResult()
    seen = 0
    r.ws()
    if r.at() != _RBRACE:
        while True:
            r.ws()
            if r.at() != _QUOTE:
                raise r.refuse("malformed")
            key = r.string()
            r.ws()
            if r.at() != _COLON:
                raise r.refuse("malformed")
            r.pos += 1
            r.ws()
            if key == "value":
                if seen & 1:
                    raise r.refuse("duplicate_field")
                seen |= 1
                v.value = _decode_actionedit(r)
            else:
                raise r.refuse("unknown_field")
            r.ws()
            if r.at() != _COMMA:
                break
            r.pos += 1
    if r.at() != _RBRACE:
        raise r.refuse("malformed")
    r.pos += 1
    r.depth -= 1
    if seen & 1 != 1:
        raise r.refuse("missing_field")
    return v


def _decode_oaauthorizationoperatorretireactionresult(r):
    if r.at() != _LBRACE:
        raise r.refuse("wrong_type")
    r.enter()
    r.pos += 1
    v = OAAuthorizationOperatorRetireActionResult()
    seen = 0
    r.ws()
    if r.at() != _RBRACE:
        while True:
            r.ws()
            if r.at() != _QUOTE:
                raise r.refuse("malformed")
            key = r.string()
            r.ws()
            if r.at() != _COLON:
                raise r.refuse("malformed")
            r.pos += 1
            r.ws()
            if key == "value":
                if seen & 1:
                    raise r.refuse("duplicate_field")
                seen |= 1
                v.value = _decode_actionedit(r)
            else:
                raise r.refuse("unknown_field")
            r.ws()
            if r.at() != _COMMA:
                break
            r.pos += 1
    if r.at() != _RBRACE:
        raise r.refuse("malformed")
    r.pos += 1
    r.depth -= 1
    if seen & 1 != 1:
        raise r.refuse("missing_field")
    return v


def decode(data):
    r = _Reader(bytes(data))
    r.ws()
    v = _decode_request(r)
    r.ws()
    if r.pos < len(r.buf):
        raise r.refuse("trailing_bytes")
    return v


# REFUSALS is in the order two of them are chosen between.

REFUSALS = ["malformed", "bad_string", "number_spelling", "wrong_type", "depth_exceeded", "duplicate_key", "duplicate_field", "unknown_field", "missing_field", "bad_enum", "trailing_bytes"]


def refusal_rank(word):
    return REFUSALS.index(word) if word in REFUSALS else -1


class DispatchError(ValueError):
    def __init__(self, code):
        super().__init__(code)
        self.code = code


def _service_decode(decoder, frame, depth=0):
    if not isinstance(frame, (bytes, bytearray, memoryview)):
        raise TypeError("transport frame must be bytes")
    r = _Reader(bytes(frame))
    r.depth = depth
    r.ws()
    value = decoder(r)
    r.ws()
    if r.pos != len(r.buf):
        raise r.refuse("trailing_bytes")
    return value


def _service_encode(encoder, value, depth):
    out = bytearray()
    try:
        encoder(out, value, depth)
    except UnicodeError:
        raise Refusal("bad_string", 0) from None
    return bytes(out)


def _service_request(service, method, arguments):
    v = OAServiceFrame(version=1, service=service, method=method, arguments=arguments)
    frame = _service_encode(enc_oaserviceframe, v, 0)
    _service_decode(_decode_oaserviceframe, frame)
    return frame


def _service_check(kind, value, depth=0):
    # Python coercions must not silently change typed service arguments.
    if depth >= _DEPTH_LIMIT and (kind.startswith(("list<", "map<")) or kind in _SERVICE_RECORDS):
        raise Refusal("depth_exceeded", 0)
    if kind == "string":
        valid = type(value) is str
    elif kind in ("i32", "i64"):
        valid = type(value) is int
        if valid:
            limit = 1 << (31 if kind == "i32" else 63)
            if not -limit <= value < limit:
                raise Refusal("number_spelling", 0)
    elif kind == "bool":
        valid = type(value) is bool
    elif kind == "json":
        valid = isinstance(value, (str, bytes, bytearray, memoryview))
    elif kind.startswith("list<"):
        valid = type(value) is list
        if valid:
            for item in value:
                _service_check(kind[5:-1], item, depth + 1)
    elif kind.startswith("map<string,"):
        valid = type(value) is dict
        if valid:
            for key, item in value.items():
                _service_check("string", key, depth + 1)
                _service_check(kind[11:-1], item, depth + 1)
    else:
        cls, fields = _SERVICE_RECORDS[kind]
        valid = isinstance(value, cls)
        if valid:
            for name, field_kind, omit in fields:
                if not hasattr(value, name):
                    raise Refusal("missing_field", 0)
                item = getattr(value, name)
                if omit == "absent" and item is None:
                    continue
                _service_check(field_kind, item, depth + 1)
    if not valid:
        raise Refusal("wrong_type", 0)

def service_name(frame):
    """Validate envelope/version for routing; dispatchers validate typed arguments."""
    value = _service_decode(_decode_oaserviceframe, frame)
    if value.version != 1:
        raise DispatchError("unknown_version")
    return value.service



class ServiceError(Exception):
    def __init__(self, code, message=""):
        super().__init__(message if message else code)
        self.code = code
        self.message = message


def _service_response(frame, service, method):
    reply = _service_decode(_decode_oaservicereply, frame)
    if reply.version != 1:
        raise DispatchError("unknown_version")
    if reply.service != service or reply.method != method:
        raise DispatchError("mismatched_response")
    if not reply.ok:
        error = _service_decode(_decode_oaserviceerror, reply.payload, 1)
        if not error.code:
            raise DispatchError("invalid_error")
        raise ServiceError(error.code, error.message)
    return reply.payload

_SERVICE_RECORDS = {
    "Request": (Request, [("op","string","never"),("name","string","absent"),("rights","list<string>","zero"),("secret","string","absent"),("token","string","absent"),("right","string","absent"),("why","string","absent"),("app","string","absent"),("admin","string","absent"),]),
    "AppMetadata": (AppMetadata, [("id","string","never"),("name","string","never"),("rights","list<string>","never"),("registered","string","never"),]),
    "HoldMetadata": (HoldMetadata, [("app","string","never"),("name","string","never"),("right","string","never"),("why","string","never"),("since","string","never"),]),
    "ResponseMetadata": (ResponseMetadata, [("code","string","absent"),("error","string","absent"),("secret","string","absent"),("token","string","absent"),("expires","string","absent"),("app","AppMetadata","absent"),("right","string","absent"),("apps","list<AppMetadata>","zero"),("holds","list<HoldMetadata>","zero"),]),
    "Subject": (Subject, [("account","string","never"),("program","string","never"),]),
    "Decision": (Decision, [("outcome","string","never"),("policy_revision","string","absent"),]),
    "PolicyRule": (PolicyRule, [("subject","Subject","never"),("action","string","never"),("resource","string","never"),("permit","bool","never"),]),
    "PolicyPage": (PolicyPage, [("outcome","string","never"),("revision","string","never"),("catalog","list<string>","never"),("rules","list<PolicyRule>","never"),("next","string","never"),("complete","bool","never"),]),
    "PolicyEdit": (PolicyEdit, [("outcome","string","never"),("revision","string","never"),("current","PolicyRule","absent"),]),
    "RuleRecord": (RuleRecord, [("rule","PolicyRule","never"),("set_by","Subject","never"),("set_at","string","never"),("why","string","never"),("expires","string","never"),]),
    "RuleRead": (RuleRead, [("outcome","string","never"),("revision","string","never"),("record","RuleRecord","absent"),]),
    "CatalogEntry": (CatalogEntry, [("action","string","never"),("registered_by","Subject","never"),("registered_at","string","never"),]),
    "ActionEdit": (ActionEdit, [("outcome","string","never"),("revision","string","never"),("current","CatalogEntry","absent"),]),
    "OAAuthorizationDecideArguments": (OAAuthorizationDecideArguments, [("action","string","never"),("resource","string","never"),]),
    "OAAuthorizationDecideForArguments": (OAAuthorizationDecideForArguments, [("subject","Subject","never"),("action","string","never"),("resource","string","never"),]),
    "OAAuthorizationOperatorListPolicyArguments": (OAAuthorizationOperatorListPolicyArguments, [("cursor","string","never"),("limit","i64","never"),]),
    "OAAuthorizationOperatorSetRuleArguments": (OAAuthorizationOperatorSetRuleArguments, [("expected_revision","string","never"),("rule","PolicyRule","never"),]),
    "OAAuthorizationOperatorRevokeRuleArguments": (OAAuthorizationOperatorRevokeRuleArguments, [("expected_revision","string","never"),("subject","Subject","never"),("action","string","never"),("resource","string","never"),]),
    "OAAuthorizationOperatorSetRuleForArguments": (OAAuthorizationOperatorSetRuleForArguments, [("expected_revision","string","never"),("rule","PolicyRule","never"),("ttl_ms","i64","never"),("why","string","never"),]),
    "OAAuthorizationOperatorReadRuleArguments": (OAAuthorizationOperatorReadRuleArguments, [("subject","Subject","never"),("action","string","never"),("resource","string","never"),]),
    "OAAuthorizationOperatorRegisterActionArguments": (OAAuthorizationOperatorRegisterActionArguments, [("expected_revision","string","never"),("action","string","never"),]),
    "OAAuthorizationOperatorRetireActionArguments": (OAAuthorizationOperatorRetireActionArguments, [("expected_revision","string","never"),("action","string","never"),]),
    "OAServiceFrame": (OAServiceFrame, [("version","i32","never"),("service","string","never"),("method","string","never"),("arguments","json","never"),]),
    "OAServiceReply": (OAServiceReply, [("version","i32","never"),("service","string","never"),("method","string","never"),("ok","bool","never"),("payload","json","never"),]),
    "OAServiceError": (OAServiceError, [("code","string","never"),("message","string","never"),]),
    "OAAuthorizationDecideResult": (OAAuthorizationDecideResult, [("value","Decision","never"),]),
    "OAAuthorizationDecideForResult": (OAAuthorizationDecideForResult, [("value","Decision","never"),]),
    "OAAuthorizationOperatorListPolicyResult": (OAAuthorizationOperatorListPolicyResult, [("value","PolicyPage","never"),]),
    "OAAuthorizationOperatorSetRuleResult": (OAAuthorizationOperatorSetRuleResult, [("value","PolicyEdit","never"),]),
    "OAAuthorizationOperatorRevokeRuleResult": (OAAuthorizationOperatorRevokeRuleResult, [("value","PolicyEdit","never"),]),
    "OAAuthorizationOperatorSetRuleForResult": (OAAuthorizationOperatorSetRuleForResult, [("value","PolicyEdit","never"),]),
    "OAAuthorizationOperatorReadRuleResult": (OAAuthorizationOperatorReadRuleResult, [("value","RuleRead","never"),]),
    "OAAuthorizationOperatorRegisterActionResult": (OAAuthorizationOperatorRegisterActionResult, [("value","ActionEdit","never"),]),
    "OAAuthorizationOperatorRetireActionResult": (OAAuthorizationOperatorRetireActionResult, [("value","ActionEdit","never"),]),
}


class Authorization:
    __doc__ = "General exact-rule authorization decision point with a bounded, registrable catalogue and persistence. Unknown, absent, unspecified, expired, failed and refused decisions never permit. Explicitly authorized operator configuration registers actions and grants/revokes exact rules; no serialized awake lease is introduced."
    def Decide(self, action: "str", resource: "str") -> "Decision":
        raise NotImplementedError
    def DecideFor(self, subject: "Subject", action: "str", resource: "str") -> "Decision":
        raise NotImplementedError


class AuthorizationClient(Authorization):
    def __init__(self, transport):
        self._transport = transport

    def Decide(self, action: "str", resource: "str") -> "Decision":
        _service_check("string", action)
        _service_check("string", resource)
        _oa_args = OAAuthorizationDecideArguments()
        _oa_args.action = action
        _oa_args.resource = resource
        _oa_arguments = _service_encode(enc_oaauthorizationdecidearguments, _oa_args, 1)
        _service_decode(_decode_oaauthorizationdecidearguments, _oa_arguments, 1)
        _oa_request = _service_request("abstraction.rights/authorization@1", "Decide", _oa_arguments)
        _oa_payload = _service_response(self._transport.exchange_frame(_oa_request), "abstraction.rights/authorization@1", "Decide")
        _oa_result = _service_decode(_decode_oaauthorizationdecideresult, _oa_payload, 1)
        return _oa_result.value

    def DecideFor(self, subject: "Subject", action: "str", resource: "str") -> "Decision":
        _service_check("Subject", subject)
        _service_check("string", action)
        _service_check("string", resource)
        _oa_args = OAAuthorizationDecideForArguments()
        _oa_args.subject = subject
        _oa_args.action = action
        _oa_args.resource = resource
        _oa_arguments = _service_encode(enc_oaauthorizationdecideforarguments, _oa_args, 1)
        _service_decode(_decode_oaauthorizationdecideforarguments, _oa_arguments, 1)
        _oa_request = _service_request("abstraction.rights/authorization@1", "DecideFor", _oa_arguments)
        _oa_payload = _service_response(self._transport.exchange_frame(_oa_request), "abstraction.rights/authorization@1", "DecideFor")
        _oa_result = _service_decode(_decode_oaauthorizationdecideforresult, _oa_payload, 1)
        return _oa_result.value


class AuthorizationOperator:
    __doc__ = "Explicitly authorized administration of the configured decision policy: catalogue registration and retirement, exact rules with expiry and provenance. Receiving same-account Program proof plus a trusted typed-peer operator callback is required; nil refuses. Policy denial is forbidden; callback/storage failure is unavailable. No caller credential, verified flag or provider path is accepted. Resource services remain the receiving enforcement points. Legacy bearer and awake lease behavior is separate."
    def ListPolicy(self, cursor: "str", limit: "int") -> "PolicyPage":
        raise NotImplementedError
    def SetRule(self, expected_revision: "str", rule: "PolicyRule") -> "PolicyEdit":
        raise NotImplementedError
    def RevokeRule(self, expected_revision: "str", subject: "Subject", action: "str", resource: "str") -> "PolicyEdit":
        raise NotImplementedError
    def SetRuleFor(self, expected_revision: "str", rule: "PolicyRule", ttl_ms: "int", why: "str") -> "PolicyEdit":
        raise NotImplementedError
    def ReadRule(self, subject: "Subject", action: "str", resource: "str") -> "RuleRead":
        raise NotImplementedError
    def RegisterAction(self, expected_revision: "str", action: "str") -> "ActionEdit":
        raise NotImplementedError
    def RetireAction(self, expected_revision: "str", action: "str") -> "ActionEdit":
        raise NotImplementedError


class AuthorizationOperatorClient(AuthorizationOperator):
    def __init__(self, transport):
        self._transport = transport

    def ListPolicy(self, cursor: "str", limit: "int") -> "PolicyPage":
        _service_check("string", cursor)
        _service_check("i64", limit)
        _oa_args = OAAuthorizationOperatorListPolicyArguments()
        _oa_args.cursor = cursor
        _oa_args.limit = limit
        _oa_arguments = _service_encode(enc_oaauthorizationoperatorlistpolicyarguments, _oa_args, 1)
        _service_decode(_decode_oaauthorizationoperatorlistpolicyarguments, _oa_arguments, 1)
        _oa_request = _service_request("abstraction.rights/operator@1", "ListPolicy", _oa_arguments)
        _oa_payload = _service_response(self._transport.exchange_frame(_oa_request), "abstraction.rights/operator@1", "ListPolicy")
        _oa_result = _service_decode(_decode_oaauthorizationoperatorlistpolicyresult, _oa_payload, 1)
        return _oa_result.value

    def SetRule(self, expected_revision: "str", rule: "PolicyRule") -> "PolicyEdit":
        _service_check("string", expected_revision)
        _service_check("PolicyRule", rule)
        _oa_args = OAAuthorizationOperatorSetRuleArguments()
        _oa_args.expected_revision = expected_revision
        _oa_args.rule = rule
        _oa_arguments = _service_encode(enc_oaauthorizationoperatorsetrulearguments, _oa_args, 1)
        _service_decode(_decode_oaauthorizationoperatorsetrulearguments, _oa_arguments, 1)
        _oa_request = _service_request("abstraction.rights/operator@1", "SetRule", _oa_arguments)
        _oa_payload = _service_response(self._transport.exchange_frame(_oa_request), "abstraction.rights/operator@1", "SetRule")
        _oa_result = _service_decode(_decode_oaauthorizationoperatorsetruleresult, _oa_payload, 1)
        return _oa_result.value

    def RevokeRule(self, expected_revision: "str", subject: "Subject", action: "str", resource: "str") -> "PolicyEdit":
        _service_check("string", expected_revision)
        _service_check("Subject", subject)
        _service_check("string", action)
        _service_check("string", resource)
        _oa_args = OAAuthorizationOperatorRevokeRuleArguments()
        _oa_args.expected_revision = expected_revision
        _oa_args.subject = subject
        _oa_args.action = action
        _oa_args.resource = resource
        _oa_arguments = _service_encode(enc_oaauthorizationoperatorrevokerulearguments, _oa_args, 1)
        _service_decode(_decode_oaauthorizationoperatorrevokerulearguments, _oa_arguments, 1)
        _oa_request = _service_request("abstraction.rights/operator@1", "RevokeRule", _oa_arguments)
        _oa_payload = _service_response(self._transport.exchange_frame(_oa_request), "abstraction.rights/operator@1", "RevokeRule")
        _oa_result = _service_decode(_decode_oaauthorizationoperatorrevokeruleresult, _oa_payload, 1)
        return _oa_result.value

    def SetRuleFor(self, expected_revision: "str", rule: "PolicyRule", ttl_ms: "int", why: "str") -> "PolicyEdit":
        _service_check("string", expected_revision)
        _service_check("PolicyRule", rule)
        _service_check("i64", ttl_ms)
        _service_check("string", why)
        _oa_args = OAAuthorizationOperatorSetRuleForArguments()
        _oa_args.expected_revision = expected_revision
        _oa_args.rule = rule
        _oa_args.ttl_ms = ttl_ms
        _oa_args.why = why
        _oa_arguments = _service_encode(enc_oaauthorizationoperatorsetruleforarguments, _oa_args, 1)
        _service_decode(_decode_oaauthorizationoperatorsetruleforarguments, _oa_arguments, 1)
        _oa_request = _service_request("abstraction.rights/operator@1", "SetRuleFor", _oa_arguments)
        _oa_payload = _service_response(self._transport.exchange_frame(_oa_request), "abstraction.rights/operator@1", "SetRuleFor")
        _oa_result = _service_decode(_decode_oaauthorizationoperatorsetruleforresult, _oa_payload, 1)
        return _oa_result.value

    def ReadRule(self, subject: "Subject", action: "str", resource: "str") -> "RuleRead":
        _service_check("Subject", subject)
        _service_check("string", action)
        _service_check("string", resource)
        _oa_args = OAAuthorizationOperatorReadRuleArguments()
        _oa_args.subject = subject
        _oa_args.action = action
        _oa_args.resource = resource
        _oa_arguments = _service_encode(enc_oaauthorizationoperatorreadrulearguments, _oa_args, 1)
        _service_decode(_decode_oaauthorizationoperatorreadrulearguments, _oa_arguments, 1)
        _oa_request = _service_request("abstraction.rights/operator@1", "ReadRule", _oa_arguments)
        _oa_payload = _service_response(self._transport.exchange_frame(_oa_request), "abstraction.rights/operator@1", "ReadRule")
        _oa_result = _service_decode(_decode_oaauthorizationoperatorreadruleresult, _oa_payload, 1)
        return _oa_result.value

    def RegisterAction(self, expected_revision: "str", action: "str") -> "ActionEdit":
        _service_check("string", expected_revision)
        _service_check("string", action)
        _oa_args = OAAuthorizationOperatorRegisterActionArguments()
        _oa_args.expected_revision = expected_revision
        _oa_args.action = action
        _oa_arguments = _service_encode(enc_oaauthorizationoperatorregisteractionarguments, _oa_args, 1)
        _service_decode(_decode_oaauthorizationoperatorregisteractionarguments, _oa_arguments, 1)
        _oa_request = _service_request("abstraction.rights/operator@1", "RegisterAction", _oa_arguments)
        _oa_payload = _service_response(self._transport.exchange_frame(_oa_request), "abstraction.rights/operator@1", "RegisterAction")
        _oa_result = _service_decode(_decode_oaauthorizationoperatorregisteractionresult, _oa_payload, 1)
        return _oa_result.value

    def RetireAction(self, expected_revision: "str", action: "str") -> "ActionEdit":
        _service_check("string", expected_revision)
        _service_check("string", action)
        _oa_args = OAAuthorizationOperatorRetireActionArguments()
        _oa_args.expected_revision = expected_revision
        _oa_args.action = action
        _oa_arguments = _service_encode(enc_oaauthorizationoperatorretireactionarguments, _oa_args, 1)
        _service_decode(_decode_oaauthorizationoperatorretireactionarguments, _oa_arguments, 1)
        _oa_request = _service_request("abstraction.rights/operator@1", "RetireAction", _oa_arguments)
        _oa_payload = _service_response(self._transport.exchange_frame(_oa_request), "abstraction.rights/operator@1", "RetireAction")
        _oa_result = _service_decode(_decode_oaauthorizationoperatorretireactionresult, _oa_payload, 1)
        return _oa_result.value
